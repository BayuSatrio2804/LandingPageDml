# DML Corporate, Plan 3: Beranda Final Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Membangun beranda sinematik final (8 dari 9 seksi master spec, seksi Artikel ditunda) di `dml-web/src/app/(site)/page.tsx`, menggantikan placeholder Plan 1, sekaligus menuntaskan tiga item triage Plan 2 yang jadi gerbang sebelum plan ini boleh mulai.

**Architecture:** Setiap seksi beranda adalah satu file di `src/features/home/`, Server Component secara default, dengan client leaf terisolasi untuk apa pun yang menyentuh GSAP, Lenis, atau R3F. `src/features/fleet/` menampung logika non-3D seksi armada (blueprint SVG, tabel spesifikasi) yang bisa dipakai ulang di luar beranda nanti, `src/features/timeline/` dan `src/features/route-map/` menampung logika seksi silsilah dan peta rute. R3F hidup satu-satunya di `src/features/home/fleet-3d/`, dimuat lewat `next/dynamic` dengan `ssr: false`, dipicu `IntersectionObserver`. Progress scroll masuk ke canvas lewat `ref` mutable yang ditulis ScrollTrigger dan dibaca `useFrame`, tidak pernah lewat React state, dan GSAP tidak pernah menyentuh objek milik canvas. Pipeline aset (`scripts/prepare-assets.ts`) mengekstrak tiga arsip drone di `assets/`, mengurasi lewat manifest eksplisit, dan menghasilkan turunan AVIF/WebP di `public/media/`, satu-satunya yang dikomit.

**Tech Stack:** Next.js 16.3, React 19, Tailwind v4, GSAP 3.13 (ScrollTrigger, SplitText, sudah terpasang), Lenis (sudah terpasang), `three`, `@react-three/fiber`, `@react-three/drei` (baru), sharp (sudah terpasang), bun, Vitest, Playwright, `@axe-core/playwright`, `@lhci/cli` (baru, Lighthouse CI).

**Spec:** `docs/superpowers/specs/2026-08-16-dml-corporate-design.md` (§7 Beranda, §9 Pipeline aset, §13 Aksesibilitas, §14 Testing) dan `docs/superpowers/specs/2026-08-17-dml-plan-3-beranda-design.md` (keputusan pull-forward, delta scope, gerbang Task 0).

## Global Constraints

Setiap task secara implisit tunduk pada seluruh isi bagian ini. Baris bertanda (Plan 1)/(Plan 2) sudah berlaku sejak plan sebelumnya dan tetap mengikat; baris bertanda (Plan 3) baru untuk plan ini.

- **Next.js minimum 16.2.0, bun sebagai satu-satunya package manager.** (Plan 1)
- **Bahasa situs Indonesia saja**, `lang="id"`, tidak ada i18n. (Plan 1)
- **Halaman dark-locked**, tidak ada `dark:` variant atau theme toggle. (Plan 1)
- **Teks di atas `--color-accent` selalu `--color-on-accent`.** Kombinasi `--color-ink` di atas `--color-accent` dilarang, 2,72:1. (Plan 1)
- **Radius: 12px kartu dan panel, 8px input, full-pill tombol.** (Plan 1)
- **Ikon hanya `@phosphor-icons/react`, `weight="regular"`.** (Plan 1)
- **Dilarang `window.addEventListener('scroll')`.** Gunakan `IntersectionObserver` atau ScrollTrigger. (Plan 1)
- **Dilarang menyimpan nilai kontinu (scroll, pointer) di React state.** (Plan 1)
- **Setiap `useEffect` yang membuat side effect lintas-render wajib punya cleanup.** GSAP: `gsap.context()` lalu `ctx.revert()`. (Plan 1)
- **Tidak ada em dash atau en dash** di teks pengguna maupun komentar kode. Gunakan tanda hubung biasa. (Plan 1)
- **Konten teks dan link selalu render di server.** Client leaf hanya mengubah transform, opacity, atau state interaktif elemen yang sudah ada di HTML. (Plan 1)
- **Angka perusahaan yang belum dikonfirmasi klien ditandai** `// unverified: <sumber>` di file data, `{/* unverified: <sumber> */}` di titik render JSX. Jangan menambah entri berisi tanggal atau angka yang tidak ada di sumber manapun. (Plan 1)
- **Setiap task wajib lolos `bun run lint`, `bunx tsc --noEmit`, `bun run test`, dan `bun run build`** sebelum commit. (Plan 1)
- **`noUncheckedIndexedAccess` aktif.** Akses indeks array/Record menghasilkan `T | undefined`; perbaiki dengan guard eksplisit, bukan `!` atau `as`. (Plan 1)
- **Tidak ada iframe atau script pihak ketiga.** (Plan 2)
- **Satu seksi beranda sama dengan satu file** di `src/features/home/`. Tidak ada `page.tsx` raksasa. (Plan 3)
- **R3F dan GSAP tidak boleh berada di tree komponen yang sama.** Seksi 3D dipin oleh ScrollTrigger dari komponen pembungkus di luar canvas; di dalam canvas hanya R3F. (Plan 3)
- **File mentah di `assets/_raw/` tidak pernah dikomit.** Hanya turunan di `public/media/` yang dikomit. Setiap gambar hasil pipeline wajib EXIF-stripped. (Plan 3)
- **Satu CTA primer per intent di seluruh beranda.** Tidak ada dua tombol dengan tujuan sama. (Plan 3)
- **Dilarang di beranda:** marquee, custom cursor, scroll cue, eyebrow bernomor seksi, dot status dekoratif, strip lokasi/cuaca, fake screenshot dari div, caption kredit foto palsu, label versi. (Plan 3)
- **CTA primer beranda (hero, seksi lini bisnis, CTA/footer) mengarah ke `/kontak` untuk sementara**, ditandai komentar `// TODO(plan-bisnis): ...` di tiap titik pemakaian, karena `/bisnis/transportasi-bbm/permintaan-informasi` belum dibangun. (Plan 3)
- **Kartu lini bisnis (seksi 3) tidak boleh punya `href` ke `/bisnis/<slug>`.** Halaman itu belum ada; link akan 404. (Plan 3)

---

## File Structure

```
dml-web/
  docker-compose.yml                       MODIFIKASI: healthcheck Postgres
  README.md                                MODIFIKASI: instruksi setup fresh clone
  lighthouserc.json                        BARU: ambang Lighthouse CI
  scripts/
    prepare-assets.ts                      BARU: pipeline ekstraksi dan konversi foto
  src/
    app/
      not-found.tsx                        BARU: 404 root-level, html/body sendiri
      (site)/
        page.tsx                           MODIFIKASI: rakit 8 seksi beranda
    components/
      ui/
        cta-link.tsx                       BARU: primitif tombol pill accent
    content/
      types.ts                             MODIFIKASI: tambah FleetClass
      fleet.ts                             BARU: data 5 kelas kapal
      fleet.test.ts
    features/
      home/
        hero.tsx                           BARU: seksi 1
        sequence/
          night-sequence.tsx               BARU: client leaf, crossfade AVIF
        day-cut.tsx                        BARU: seksi 2
        business-lines.tsx                 BARU: seksi 3
        fleet-comparator.tsx               BARU: seksi 4, wrapper pin + dynamic import
        fleet-3d/
          fleet-canvas.tsx                 BARU: client leaf, R3F Canvas
          hull-geometry.ts                 BARU: builder geometri primitif
        route-map.tsx                      BARU: seksi 5
        lineage.tsx                        BARU: seksi 6
        certifications.tsx                 BARU: seksi 7
        cta-section.tsx                    BARU: seksi 9
      fleet/
        blueprint-svg.tsx                  BARU: fallback mobile <768px
        spec-table.tsx                     BARU: tabel a11y screen reader
      route-map/
        ports.ts                           BARU: data 4 pelabuhan
      timeline/
        lineage-pan.tsx                    BARU: client leaf, horizontal pan
    lib/
      media/
        manifest.ts                        BARU: manifest frame terkurasi
        manifest.test.ts
      motion/
        use-counter.ts                     BARU: hook IntersectionObserver + counter
  public/
    media/
      hero-malam/                          BARU: 10 frame AVIF/WebP, 4 lebar
      hari/                                BARU: 1 frame wide anchorage
      lini-bisnis/                         BARU: 3 frame kartu
  tests/
    e2e/
      beranda.spec.ts                      BARU: reduced-motion, JS-off, section content
      no-js.spec.ts                        MODIFIKASI: assert 8 seksi hadir tanpa JS
```

---

### Task 1: Satu sumber nomor WhatsApp

**Files:**
- Modify: `dml-web/src/content/company.ts`
- Modify: `dml-web/src/content/types.ts`
- Modify: `dml-web/src/app/(site)/kontak/page.tsx`
- Test: `dml-web/src/content/company.test.ts`

**Interfaces:**
- Consumes: tidak ada
- Produces: `COMPANY.whatsapp: string` (format E.164 tanpa `+`, contoh `"625113268280"`, dipakai `wa.me`). `/kontak` dan `/karier` membaca field ini, bukan `WHATSAPP_NUMBER` maupun `COMPANY.phone` untuk link WhatsApp.

Sebelum task ini, `/kontak` membaca `process.env.WHATSAPP_NUMBER` (env var terpisah) sementara `/karier` membaca `COMPANY.phone` (nomor kantor, format `+625113268280`). Keduanya kebetulan sama sekarang, tapi tidak ditegakkan. `COMPANY.phone` tetap jadi nomor kantor untuk JSON-LD `Organization`, `COMPANY.whatsapp` jadi field baru khusus link `wa.me`, satu sumber untuk kedua halaman.

- [ ] **Step 1: Baca pemakaian saat ini**

Run: `grep -rn "WHATSAPP_NUMBER" dml-web/src`

Catat baris persis yang memakai `process.env.WHATSAPP_NUMBER` di `dml-web/src/app/(site)/kontak/page.tsx` sebelum mengubahnya.

- [ ] **Step 2: Tulis tes yang gagal**

Create `dml-web/src/content/company.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { COMPANY } from "./company";

describe("COMPANY.whatsapp", () => {
  it("hanya berisi digit, tanpa tanda plus atau spasi", () => {
    expect(COMPANY.whatsapp).toMatch(/^\d+$/);
  });

  it("sama dengan COMPANY.phone tanpa karakter non-digit", () => {
    expect(COMPANY.whatsapp).toBe(COMPANY.phone.replace(/\D/g, ""));
  });
});
```

- [ ] **Step 3: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/content/company.test.ts`
Expected: FAIL, `COMPANY.whatsapp` bukan properti yang ada.

- [ ] **Step 4: Tambah tipe dan field**

Modify `dml-web/src/content/types.ts`, tambahkan `whatsapp: string;` ke `type Company` setelah `phone: string;`.

Modify `dml-web/src/content/company.ts`, tambahkan setelah `phone: "+625113268280",`:

```ts
  whatsapp: "625113268280", // unverified: SinarAlam Corporation, format E.164 tanpa tanda plus untuk wa.me
```

- [ ] **Step 5: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/content/company.test.ts`
Expected: PASS, dua tes hijau.

- [ ] **Step 6: Ganti pemakaian di `/kontak`**

Modify `dml-web/src/app/(site)/kontak/page.tsx`. Hapus baris yang membaca `process.env.WHATSAPP_NUMBER` (termasuk `throw` kalau kosong, ditambahkan commit `07f8938`), ganti dengan import `COMPANY` dan pakai `COMPANY.whatsapp` langsung di tautan `https://wa.me/${COMPANY.whatsapp}`. Karena `COMPANY.whatsapp` sekarang konstanta modul, bukan env var yang bisa kosong di build, hapus juga guard `if (!whatsappNumber) throw ...` yang sudah tidak relevan.

Modify `dml-web/tests/e2e/kontak.spec.ts` kalau ada assert yang membaca `process.env.WHATSAPP_NUMBER`, ganti jadi assert terhadap `COMPANY.whatsapp` yang diimpor di test.

- [ ] **Step 7: Verifikasi tidak ada sisa referensi env var**

Run: `grep -rn "WHATSAPP_NUMBER" dml-web/src dml-web/tests dml-web/.env.local.example 2>/dev/null`
Expected: tidak ada hasil (kalau `.env.local.example` mendaftarkan `WHATSAPP_NUMBER`, hapus barisnya di Task 2).

- [ ] **Step 8: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos.

- [ ] **Step 9: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/content/company.ts dml-web/src/content/types.ts dml-web/src/content/company.test.ts dml-web/src/app/\(site\)/kontak/page.tsx dml-web/tests/e2e/kontak.spec.ts
git commit -m "fix: satukan sumber nomor WhatsApp ke COMPANY.whatsapp"
```

---

### Task 2: Dokumentasi setup dan healthcheck Postgres

**Files:**
- Modify: `dml-web/README.md`
- Modify: `dml-web/docker-compose.yml`

**Interfaces:**
- Consumes: tidak ada
- Produces: tidak ada (dokumentasi dan konfigurasi, tidak ada API baru)

- [ ] **Step 1: Baca docker-compose.yml saat ini**

Run: `cat dml-web/docker-compose.yml`

- [ ] **Step 2: Tambah healthcheck**

Modify `dml-web/docker-compose.yml`, tambahkan blok `healthcheck` di service Postgres (nama service persis seperti yang sudah ada, biasanya `postgres` atau `db`):

```yaml
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 5s
      retries: 10
```

Sesuaikan `-U postgres` dengan `POSTGRES_USER` yang benar-benar dipakai di file itu (baca dari `environment:` service yang sama).

- [ ] **Step 3: Tulis README setup**

Modify `dml-web/README.md`, ganti seluruh isi teks stock `create-next-app` dengan:

```markdown
# dml-web

Situs company profile PT Dutabahari Menara Line. Next.js 16.3 App Router, Payload CMS 3 (Postgres), Tailwind v4.

## Setup fresh clone

1. `bun install`
2. Salin `.env.local.example` jadi `.env.local`, isi setiap variabel (lihat komentar di file itu).
3. `docker compose up -d` dan tunggu healthcheck Postgres lolos sebelum lanjut:
   ```bash
   docker compose up -d
   until docker compose ps --format json | grep -q '"Health":"healthy"'; do sleep 1; done
   ```
4. `bun run payload migrate`
5. `bun run dev`, buka `http://localhost:3000`

## Perintah penting

- `bun run check`: lint, typecheck, test, build, doctor, lighthouse berurutan. Gerbang wajib sebelum deploy.
- `bun run test:e2e`: Playwright, butuh `bun run build && bun run start` (otomatis lewat `playwright.config.ts`).
- `bun run payload migrate`: jalankan migrasi Payload. Jangan pernah mengandalkan dev-mode schema push, lihat `payload.config.ts` (`push: false`).

## Struktur

- `src/app/(site)/`: halaman publik.
- `src/app/(payload)/`: admin panel Payload di `/admin`.
- `src/payload/`: config dan collection Payload.
- `src/features/`: logika per fitur (form inquiry, seksi beranda).
- `src/content/`: data korporat hardcoded, wajib ditandai `// unverified: <sumber>` untuk angka yang belum dikonfirmasi klien.
```

Kalau `.env.local.example` belum ada, buat dulu (Step 4) sebelum menulis Step 3 secara final, karena README merujuk file itu.

- [ ] **Step 4: Pastikan `.env.local.example` ada dan lengkap**

Run: `ls dml-web/.env.local.example 2>/dev/null || echo "belum ada"`

Kalau belum ada, buat `dml-web/.env.local.example` berisi setiap variabel yang dibaca `payload.config.ts` dan `docker-compose.yml` (cek dengan `grep -rn "process.env\." dml-web/src/payload dml-web/docker-compose.yml`), masing-masing dengan komentar satu baris penjelasan, tanpa nilai rahasia asli.

- [ ] **Step 5: Verifikasi prosedur README dari kondisi kosong**

Run:
```bash
cd dml-web
docker compose down -v
docker compose up -d
until docker compose ps --format json | grep -q '"Health":"healthy"'; do sleep 1; done
bun run payload migrate
```
Expected: migrate berhasil tanpa race condition, `payload_migrations` berisi 3 baris seperti sebelumnya.

- [ ] **Step 6: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/README.md dml-web/docker-compose.yml dml-web/.env.local.example
git commit -m "docs: instruksi setup fresh clone dan healthcheck Postgres"
```

---

### Task 3: 404 root-level bergaya situs

**Files:**
- Create: `dml-web/src/app/not-found.tsx`
- Test: `dml-web/tests/e2e/not-found.spec.ts`

**Interfaces:**
- Consumes: `fontVariables` dari `@/lib/fonts`, `TOKENS` tidak diperlukan langsung (dipakai lewat Tailwind class)
- Produces: tidak ada API baru, ini root-level fallback

`app/layout.tsx` dihapus di Plan 2 (commit `390d64f`, merge multiple-root-layouts). Tidak ada `not-found.tsx`/`error.tsx`/`global-error.tsx` di manapun, jadi 404 bawaan Next render dokumen kosong tanpa `lang`, tanpa CSS/font situs. `not-found.tsx` di `src/app/` (root, di luar route group manapun) jadi root layout independen untuk kasus ini, sama seperti `(payload)/layout.tsx` independen dari `(site)/layout.tsx`.

- [ ] **Step 1: Tulis komponen**

Create `dml-web/src/app/not-found.tsx`:

```tsx
import Link from "next/link";
import { fontVariables } from "@/lib/fonts";
import "./globals.css";

export default function NotFound() {
  return (
    <html lang="id" className={`${fontVariables} antialiased`}>
      <body className="flex min-h-dvh flex-col items-center justify-center bg-surface px-4 text-center text-ink">
        <p className="font-mono text-sm text-ink-muted">404</p>
        <h1 className="mt-2 font-display text-3xl font-bold md:text-4xl">
          Halaman tidak ditemukan
        </h1>
        <p className="mt-3 max-w-[45ch] text-ink-muted">
          Halaman yang dicari mungkin sudah pindah atau alamatnya salah ketik.
        </p>
        <Link
          href="/"
          className="mt-8 inline-flex items-center justify-center rounded-full bg-accent px-6 py-3 text-sm font-medium text-on-accent transition-colors hover:bg-accent-hover"
        >
          Kembali ke beranda
        </Link>
      </body>
    </html>
  );
}
```

- [ ] **Step 2: Tulis spec e2e**

Create `dml-web/tests/e2e/not-found.spec.ts`:

```ts
import { test, expect } from "@playwright/test";

test("404 render dokumen bergaya situs dengan lang dan link kembali", async ({ page }) => {
  const response = await page.goto("/halaman-yang-tidak-ada");
  expect(response?.status()).toBe(404);

  await expect(page.locator("html")).toHaveAttribute("lang", "id");
  await expect(page.getByRole("heading", { level: 1 })).toHaveText("Halaman tidak ditemukan");
  await expect(page.getByRole("link", { name: "Kembali ke beranda" })).toHaveAttribute("href", "/");

  const bodyBg = await page.evaluate(() => getComputedStyle(document.body).backgroundColor);
  expect(bodyBg).toBe("rgb(10, 20, 24)");
});
```

- [ ] **Step 3: Jalankan spec**

Run: `cd dml-web && bun run test:e2e tests/e2e/not-found.spec.ts`
Expected: PASS, dua assertion hijau, status 404 terkonfirmasi.

- [ ] **Step 4: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run build`
Expected: semua lolos.

- [ ] **Step 5: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/app/not-found.tsx dml-web/tests/e2e/not-found.spec.ts
git commit -m "fix: 404 root-level bergaya situs dengan lang dan CSS sendiri"
```

---

### Task 4: Pipeline aset foto drone

**Files:**
- Create: `dml-web/scripts/prepare-assets.ts`
- Create: `dml-web/src/lib/media/manifest.ts`
- Create: `dml-web/src/lib/media/manifest.test.ts`
- Modify: `dml-web/package.json` (script `prepare-assets`, tambah `.gitignore` entry)
- Modify: `dml-web/.gitignore`

**Interfaces:**
- Consumes: tidak ada
- Produces: `MEDIA: Record<MediaSetId, MediaAsset>` dari `@/lib/media/manifest`, tipe `MediaAsset = { id: string; basePath: string; widths: readonly number[]; alt: string }` dan helper `avifSrcSet(asset: MediaAsset): string`, `avifSrc(asset: MediaAsset, width: number): string`. Task 7 (hero), Task 8 (day cut), Task 9 (business lines) mengimpor `MEDIA` dan kedua helper ini. File fisik di `public/media/` dari menjalankan `bun run prepare-assets`.

Ini task infrastruktur, bukan konten sekali jalan. Skrip harus bisa dijalankan ulang idempoten begitu manifest berubah.

- [ ] **Step 1: Cek isi arsip**

Run:
```bash
cd /home/waxarsatia/test/company-profile
unzip -l "assets/STS SRI YULIANI.zip" | grep -E "DJI_08(1[1-9]|20)" 
unzip -l "assets/STS 06 JULI 2025.zip" | head -20
unzip -l "assets/KAPAL KAPAL.zip" | head -60
```

Catat nama file persis (case, ekstensi) untuk cluster hero `DJI_0811` sampai `DJI_0820` (dari `STS SRI YULIANI.zip` per master spec baris 466-469), satu frame wide anchorage untuk seksi day-cut (cari di `STS 06 JULI 2025.zip`, operasi STS siang), dan calon 3 foto kartu lini bisnis dari `KAPAL KAPAL.zip` (53 file, armada termasuk ro-ro di ramp pelabuhan).

- [ ] **Step 2: Ekstrak dan review visual**

Run:
```bash
mkdir -p assets/_raw
unzip -o "assets/STS SRI YULIANI.zip" -d assets/_raw/sts-sri-yuliani
unzip -o "assets/STS 06 JULI 2025.zip" -d assets/_raw/sts-06-juli
unzip -o "assets/KAPAL KAPAL.zip" -d assets/_raw/kapal-kapal
```

Pakai tool Read pada 3-5 kandidat file JPEG di `assets/_raw/kapal-kapal/` untuk memilih satu foto tanker/oil-barge, satu foto ro-ro (KMP Jambo di ramp), dan satu foto galangan/tugboat, masing-masing dengan komposisi yang jelas (kapal utuh terlihat, bukan crop parsial), untuk tiga kartu lini bisnis Task 9. Catat nama filenya.

- [ ] **Step 3: Tulis tes yang gagal untuk manifest**

Create `dml-web/src/lib/media/manifest.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { MEDIA, avifSrc, avifSrcSet } from "./manifest";

describe("MEDIA manifest", () => {
  it("hero-malam punya 10 frame", () => {
    expect(MEDIA["hero-malam"]).toHaveLength(10);
  });

  it("setiap frame punya alt text bahasa Indonesia non-kosong", () => {
    for (const set of Object.values(MEDIA)) {
      for (const asset of set) {
        expect(asset.alt.length).toBeGreaterThan(0);
      }
    }
  });

  it("avifSrc merangkai basePath, lebar, dan ekstensi avif", () => {
    const asset = MEDIA["hari"][0];
    expect(asset).toBeDefined();
    if (!asset) return;
    expect(avifSrc(asset, 1600)).toBe(`${asset.basePath}-1600.avif`);
  });

  it("avifSrcSet mencakup keempat lebar", () => {
    const asset = MEDIA["hari"][0];
    expect(asset).toBeDefined();
    if (!asset) return;
    const srcSet = avifSrcSet(asset);
    for (const width of asset.widths) {
      expect(srcSet).toContain(`${width}w`);
    }
  });
});
```

- [ ] **Step 4: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/lib/media/manifest.test.ts`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 5: Tulis manifest**

Create `dml-web/src/lib/media/manifest.ts`. Isi array `hero-malam` dengan 10 entri memakai nama file persis dari Step 1 (`DJI_0811` sampai `DJI_0820`), `hari` dengan 1 entri (wide anchorage), `lini-bisnis` dengan 3 entri (hasil pilihan Step 2). `basePath` mengikuti pola `/media/<set>/<slug>` tanpa lebar/ekstensi, ditambahkan oleh `avifSrc`/`avifSrcSet` saat dipakai:

```ts
export type MediaSetId = "hero-malam" | "hari" | "lini-bisnis";

export type MediaAsset = {
  id: string;
  basePath: string;
  widths: readonly number[];
  alt: string;
};

const STANDARD_WIDTHS = [640, 1080, 1600, 2400] as const;

export const MEDIA: Record<MediaSetId, MediaAsset[]> = {
  "hero-malam": [
    { id: "dji-0811", basePath: "/media/hero-malam/dji-0811", widths: STANDARD_WIDTHS, alt: "Orbit malam dua kapal STS di perairan Kalimantan, lampu deck menyala" },
    { id: "dji-0812", basePath: "/media/hero-malam/dji-0812", widths: STANDARD_WIDTHS, alt: "Orbit malam dua kapal STS, sudut kamera bergeser" },
    { id: "dji-0813", basePath: "/media/hero-malam/dji-0813", widths: STANDARD_WIDTHS, alt: "Orbit malam dua kapal STS, sudut kamera bergeser lanjut" },
    { id: "dji-0814", basePath: "/media/hero-malam/dji-0814", widths: STANDARD_WIDTHS, alt: "Orbit malam dua kapal STS mendekati sisi lambung" },
    { id: "dji-0815", basePath: "/media/hero-malam/dji-0815", widths: STANDARD_WIDTHS, alt: "Orbit malam dua kapal STS, posisi tengah orbit" },
    { id: "dji-0816", basePath: "/media/hero-malam/dji-0816", widths: STANDARD_WIDTHS, alt: "Orbit malam dua kapal STS melewati haluan" },
    { id: "dji-0817", basePath: "/media/hero-malam/dji-0817", widths: STANDARD_WIDTHS, alt: "Orbit malam dua kapal STS, sudut buritan" },
    { id: "dji-0818", basePath: "/media/hero-malam/dji-0818", widths: STANDARD_WIDTHS, alt: "Orbit malam dua kapal STS menjauh dari buritan" },
    { id: "dji-0819", basePath: "/media/hero-malam/dji-0819", widths: STANDARD_WIDTHS, alt: "Orbit malam dua kapal STS, sudut kamera hampir kembali" },
    { id: "dji-0820", basePath: "/media/hero-malam/dji-0820", widths: STANDARD_WIDTHS, alt: "Orbit malam dua kapal STS menutup lingkaran orbit" },
  ],
  hari: [
    { id: "dji-0710", basePath: "/media/hari/dji-0710", widths: STANDARD_WIDTHS, alt: "Area labuh jangkar armada dari udara pada siang hari" },
  ],
  "lini-bisnis": [
    { id: "transportasi-bbm", basePath: "/media/lini-bisnis/transportasi-bbm", widths: STANDARD_WIDTHS, alt: "Motor tanker PT Dutabahari Menara Line berlayar membawa BBM" },
    { id: "penumpang-roro", basePath: "/media/lini-bisnis/penumpang-roro", widths: STANDARD_WIDTHS, alt: "KMP Jambo bersandar di ramp pelabuhan penyeberangan" },
    { id: "galangan-kapal", basePath: "/media/lini-bisnis/galangan-kapal", widths: STANDARD_WIDTHS, alt: "Aktivitas perawatan kapal di galangan Pasir Mas" },
  ],
};

export function avifSrc(asset: MediaAsset, width: number): string {
  return `${asset.basePath}-${width}.avif`;
}

export function avifSrcSet(asset: MediaAsset): string {
  return asset.widths.map((width) => `${avifSrc(asset, width)} ${width}w`).join(", ");
}

export function webpSrc(asset: MediaAsset, width: number): string {
  return `${asset.basePath}-${width}.webp`;
}
```

Ganti nama file placeholder di atas (`dji-0811` dst) sesuai temuan Step 1 kalau penamaan asli berbeda kapitalisasi atau nomor urut.

- [ ] **Step 6: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/lib/media/manifest.test.ts`
Expected: PASS, empat tes hijau.

- [ ] **Step 7: Tulis skrip pipeline**

Create `dml-web/scripts/prepare-assets.ts`:

```ts
#!/usr/bin/env bun
import { mkdir } from "node:fs/promises";
import { existsSync } from "node:fs";
import sharp from "sharp";
import { MEDIA, type MediaAsset } from "../src/lib/media/manifest";

const RAW_DIR = new URL("../../assets/_raw/", import.meta.url).pathname;
const PUBLIC_MEDIA_DIR = new URL("../public/media/", import.meta.url).pathname;

/**
 * Peta manual dari basePath manifest ke lokasi file mentah hasil unzip.
 * Diisi tangan karena kurasi frame adalah keputusan manusia, bukan
 * sesuatu yang bisa ditebak dari nama arsip.
 */
const RAW_SOURCE: Record<string, string> = {
  "/media/hero-malam/dji-0811": "sts-sri-yuliani/DJI_0811.JPG",
  "/media/hero-malam/dji-0812": "sts-sri-yuliani/DJI_0812.JPG",
  "/media/hero-malam/dji-0813": "sts-sri-yuliani/DJI_0813.JPG",
  "/media/hero-malam/dji-0814": "sts-sri-yuliani/DJI_0814.JPG",
  "/media/hero-malam/dji-0815": "sts-sri-yuliani/DJI_0815.JPG",
  "/media/hero-malam/dji-0816": "sts-sri-yuliani/DJI_0816.JPG",
  "/media/hero-malam/dji-0817": "sts-sri-yuliani/DJI_0817.JPG",
  "/media/hero-malam/dji-0818": "sts-sri-yuliani/DJI_0818.JPG",
  "/media/hero-malam/dji-0819": "sts-sri-yuliani/DJI_0819.JPG",
  "/media/hero-malam/dji-0820": "sts-sri-yuliani/DJI_0820.JPG",
  "/media/hari/dji-0710": "sts-06-juli/DJI_0710.JPG",
  "/media/lini-bisnis/transportasi-bbm": "kapal-kapal/PLACEHOLDER-TANKER.JPG",
  "/media/lini-bisnis/penumpang-roro": "kapal-kapal/PLACEHOLDER-RORO.JPG",
  "/media/lini-bisnis/galangan-kapal": "kapal-kapal/PLACEHOLDER-GALANGAN.JPG",
};

async function processAsset(asset: MediaAsset): Promise<void> {
  const rawRelative = RAW_SOURCE[asset.basePath];
  if (!rawRelative) {
    throw new Error(`Tidak ada pemetaan RAW_SOURCE untuk ${asset.basePath}`);
  }
  const rawPath = `${RAW_DIR}${rawRelative}`;
  if (!existsSync(rawPath)) {
    throw new Error(`File mentah tidak ditemukan: ${rawPath}`);
  }

  const outDir = `${PUBLIC_MEDIA_DIR}${asset.basePath.replace("/media/", "")}`.replace(/\/[^/]+$/, "");
  await mkdir(outDir, { recursive: true });

  const outBase = `${PUBLIC_MEDIA_DIR}${asset.basePath.replace("/media/", "")}`;

  for (const width of asset.widths) {
    const pipeline = sharp(rawPath).rotate().resize({ width, withoutEnlargement: true }).withMetadata({ exif: {} });
    await pipeline.clone().avif({ quality: 55 }).toFile(`${outBase}-${width}.avif`);
    await pipeline.clone().webp({ quality: 65 }).toFile(`${outBase}-${width}.webp`);
  }

  console.log(`OK ${asset.basePath} (${asset.widths.length} lebar x 2 format)`);
}

async function main(): Promise<void> {
  const allAssets = Object.values(MEDIA).flat();
  for (const asset of allAssets) {
    await processAsset(asset);
  }
  console.log(`Selesai. ${allAssets.length} aset diproses.`);
}

main().catch((error: unknown) => {
  console.error(error);
  process.exit(1);
});
```

Ganti tiga baris `PLACEHOLDER-*` di `RAW_SOURCE` dengan nama file asli hasil pilihan Step 2 sebelum menjalankan skrip.

`withMetadata({ exif: {} })` menimpa blok EXIF dengan objek kosong, sharp tidak menyalin GPS/koordinat dari sumber ke output. Ini memenuhi Global Constraint strip-EXIF.

- [ ] **Step 8: Daftarkan skrip dan gitignore**

Modify `dml-web/package.json`, tambahkan di `"scripts"`:

```json
    "prepare-assets": "bun scripts/prepare-assets.ts",
```

Modify `dml-web/.gitignore` (atau `.gitignore` di root repo kalau `assets/` ada di root, bukan di `dml-web/`, sesuaikan dengan lokasi sebenarnya), tambahkan:

```
assets/_raw/
```

- [ ] **Step 9: Jalankan pipeline sungguhan**

Run: `cd dml-web && bun run prepare-assets`
Expected: 14 baris `OK ...`, lalu `Selesai. 14 aset diproses.` Verifikasi dengan `ls -la public/media/hero-malam/ | wc -l` (harus 21: 10 frame x 2 format + 1 baris `total`).

- [ ] **Step 10: Verifikasi EXIF benar-benar terstrip**

Run: `exiftool public/media/hero-malam/dji-0811-1600.avif 2>/dev/null | grep -i gps || echo "tidak ada GPS, aman"`
Expected: `tidak ada GPS, aman`. Kalau `exiftool` tidak terpasang di environment, lewati step ini dan catat di laporan task bahwa verifikasi manual EXIF perlu dilakukan sebelum deploy.

- [ ] **Step 11: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos.

- [ ] **Step 12: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/scripts/prepare-assets.ts dml-web/src/lib/media/manifest.ts dml-web/src/lib/media/manifest.test.ts dml-web/package.json dml-web/.gitignore dml-web/public/media
git commit -m "feat: pipeline aset foto drone, 14 frame AVIF/WebP EXIF-stripped"
```

---

### Task 5: Data 5 kelas kapal untuk fleet comparator

**Files:**
- Create: `dml-web/src/content/fleet.ts`
- Create: `dml-web/src/content/fleet.test.ts`
- Modify: `dml-web/src/content/types.ts`

**Interfaces:**
- Consumes: tidak ada
- Produces: `FLEET_CLASSES: FleetClass[]`, `type FleetClass = { slug: string; name: string; category: string; lengthMeters: number; beamMeters: number; dwt: number | null; capacityLabel: string; passengerCapacity: number | null; altText: string }`. Task 11 (fleet-3d geometry), Task 12 (blueprint SVG + tabel a11y) mengimpor array ini sebagai satu-satunya sumber data ketiga representasi.

Sebelum menulis angka, coba riset dulu spesifikasi real per kelas dari sumber publik senada yang sudah dipakai project ini (SinarAlam Corporation, ptdml.com, MagicPort, arsip Banjarmasin Post). Kalau ditemukan, pakai angka itu. Kalau tidak, pakai estimasi di bawah dan tandai `// unverified`. Satu kelas (Ro-Ro Ferry) sudah punya data real dari `docs/superpowers/specs/2026-08-16-dml-corporate-design.md` baris 47-52.

- [ ] **Step 1: Riset singkat**

Cari (lewat WebSearch kalau tersedia, atau catat sebagai belum ditemukan) dimensi motor tanker, oil barge, SPOB, dan tugboat milik PT Dutabahari Menara Line secara spesifik. Kalau tidak ada hasil yang bisa disitasi dengan sumber jelas dalam waktu wajar, lanjut ke Step 2 dengan estimasi.

- [ ] **Step 2: Tulis tipe**

Modify `dml-web/src/content/types.ts`, tambahkan di akhir file:

```ts
export type FleetClass = {
  slug: string;
  name: string;
  category: string;
  lengthMeters: number;
  beamMeters: number;
  dwt: number | null;
  capacityLabel: string;
  passengerCapacity: number | null;
  altText: string;
};
```

- [ ] **Step 3: Tulis tes yang gagal**

Create `dml-web/src/content/fleet.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { FLEET_CLASSES } from "./fleet";

describe("FLEET_CLASSES", () => {
  it("berisi tepat 5 kelas", () => {
    expect(FLEET_CLASSES).toHaveLength(5);
  });

  it("setiap kelas punya slug unik", () => {
    const slugs = FLEET_CLASSES.map((entry) => entry.slug);
    expect(new Set(slugs).size).toBe(slugs.length);
  });

  it("panjang dan lebar setiap kelas positif", () => {
    for (const entry of FLEET_CLASSES) {
      expect(entry.lengthMeters).toBeGreaterThan(0);
      expect(entry.beamMeters).toBeGreaterThan(0);
    }
  });

  it("kelas ro-ro-ferry punya kapasitas penumpang, kelas kargo tidak", () => {
    const ferry = FLEET_CLASSES.find((entry) => entry.slug === "ro-ro-ferry");
    expect(ferry?.passengerCapacity).toBeGreaterThan(0);

    const cargoClasses = FLEET_CLASSES.filter((entry) => entry.slug !== "ro-ro-ferry");
    for (const entry of cargoClasses) {
      expect(entry.passengerCapacity).toBeNull();
    }
  });
});
```

- [ ] **Step 4: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/content/fleet.test.ts`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 5: Implementasi**

Create `dml-web/src/content/fleet.ts`. Kalau Step 1 menemukan data real, ganti angka di bawah dan komentar sumbernya; kalau tidak, pakai persis ini:

```ts
import type { FleetClass } from "./types";

/**
 * Lima kelas dipakai fleet comparator 3D (spec bagian 7.4). Panjang dan
 * kapasitas kelas Motor Tanker, Oil Barge, SPOB, Tugboat adalah estimasi
 * proporsional dari kategori kapasitas liter yang sudah tersitasi di
 * docs/superpowers/specs/2026-08-16-dml-corporate-design.md bagian 2, karena
 * belum ada sumber publik yang merinci dimensi per kelas. DWT per kelas
 * adalah pembagian proporsional dari total armada (40.546 DWT, 15 kapal),
 * bukan angka yang tersitasi langsung. Kelas Ro-Ro Ferry memakai data real
 * dari spec yang sama (Jambo VIII/X, 68 m, sekitar 400 penumpang).
 * Wajib dikonfirmasi klien sebelum situs live.
 */
export const FLEET_CLASSES: FleetClass[] = [
  {
    slug: "motor-tanker",
    name: "Motor Tanker",
    category: "Transportasi BBM",
    lengthMeters: 95, // unverified: estimasi proporsional dari kapasitas 8 juta liter
    beamMeters: 16, // unverified: estimasi proporsional
    dwt: 9500, // unverified: estimasi proporsional dari total armada
    capacityLabel: "hingga 8 juta liter",
    passengerCapacity: null,
    altText: "Blueprint skematik motor tanker, kelas terbesar armada BBM",
  },
  {
    slug: "oil-barge",
    name: "Oil Barge",
    category: "Transportasi BBM",
    lengthMeters: 75, // unverified: estimasi proporsional dari kapasitas 4,7 juta liter
    beamMeters: 14, // unverified: estimasi proporsional
    dwt: 5600, // unverified: estimasi proporsional dari total armada
    capacityLabel: "hingga 4,7 juta liter",
    passengerCapacity: null,
    altText: "Blueprint skematik oil barge, ditarik tugboat pendamping",
  },
  {
    slug: "spob",
    name: "SPOB",
    category: "Transportasi BBM",
    lengthMeters: 55, // unverified: estimasi proporsional dari kapasitas 1,6 juta liter
    beamMeters: 11, // unverified: estimasi proporsional
    dwt: 1900, // unverified: estimasi proporsional dari total armada
    capacityLabel: "hingga 1,6 juta liter",
    passengerCapacity: null,
    altText: "Blueprint skematik SPOB, kelas terkecil armada tanker",
  },
  {
    slug: "tugboat",
    name: "Tugboat",
    category: "Transportasi BBM",
    lengthMeters: 32, // unverified: estimasi umum tugboat pendamping oil barge
    beamMeters: 9, // unverified: estimasi umum
    dwt: 450, // unverified: estimasi proporsional dari total armada
    capacityLabel: "pendamping oil barge",
    passengerCapacity: null,
    altText: "Blueprint skematik tugboat pendamping oil barge",
  },
  {
    slug: "ro-ro-ferry",
    name: "Ro-Ro Ferry (KMP Jambo X)",
    category: "Penumpang Ro-Ro",
    lengthMeters: 68, // spec: docs/superpowers/specs/2026-08-16-dml-corporate-design.md baris 49-50
    beamMeters: 17, // unverified: estimasi rasio umum ferry ro-ro sepanjang 68 m
    dwt: null,
    capacityLabel: "sekitar 400 penumpang",
    passengerCapacity: 400, // spec: docs/superpowers/specs/2026-08-16-dml-corporate-design.md baris 50
    altText: "Blueprint skematik KMP Jambo X, ferry ro-ro penumpang",
  },
];
```

- [ ] **Step 6: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/content/fleet.test.ts`
Expected: PASS, empat tes hijau.

- [ ] **Step 7: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos.

- [ ] **Step 8: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/content/fleet.ts dml-web/src/content/fleet.test.ts dml-web/src/content/types.ts
git commit -m "feat: data 5 kelas kapal untuk fleet comparator, ditandai unverified"
```

---

### Task 6: Primitif tombol CTA

**Files:**
- Create: `dml-web/src/components/ui/cta-link.tsx`
- Test: `dml-web/src/components/ui/cta-link.test.tsx`

**Interfaces:**
- Consumes: tidak ada
- Produces: `<CtaLink href={string} variant="filled" | "ghost">{children}</CtaLink>`. Task 7 (hero), Task 9 (business lines), Task 16 (CTA/footer) memakai komponen ini untuk seluruh CTA primer beranda.

- [ ] **Step 1: Tulis tes yang gagal**

Create `dml-web/src/components/ui/cta-link.test.tsx`:

```tsx
import { describe, expect, it } from "vitest";
import { render, screen } from "@testing-library/react";
import { CtaLink } from "./cta-link";

describe("CtaLink", () => {
  it("render sebagai link dengan href yang diberikan", () => {
    render(<CtaLink href="/kontak">Hubungi Kami</CtaLink>);
    const link = screen.getByRole("link", { name: "Hubungi Kami" });
    expect(link).toHaveAttribute("href", "/kontak");
  });

  it("variant filled memakai teks on-accent, bukan ink, di atas latar accent", () => {
    render(<CtaLink href="/kontak">Hubungi Kami</CtaLink>);
    const link = screen.getByRole("link", { name: "Hubungi Kami" });
    expect(link.className).toContain("bg-accent");
    expect(link.className).toContain("text-on-accent");
    expect(link.className).not.toContain("text-ink ");
  });

  it("variant ghost tidak memakai latar accent", () => {
    render(
      <CtaLink href="/kontak" variant="ghost">
        Hubungi Kami
      </CtaLink>,
    );
    const link = screen.getByRole("link", { name: "Hubungi Kami" });
    expect(link.className).not.toContain("bg-accent");
  });
});
```

- [ ] **Step 2: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/components/ui/cta-link.test.tsx`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 3: Implementasi**

Create `dml-web/src/components/ui/cta-link.tsx`:

```tsx
import Link from "next/link";

const BASE = "inline-flex items-center justify-center gap-2 rounded-full px-6 py-3 text-sm font-medium transition-colors whitespace-nowrap";

const VARIANTS = {
  filled: `${BASE} bg-accent text-on-accent hover:bg-accent-hover active:bg-accent-press`,
  ghost: `${BASE} border border-ink/30 text-ink hover:border-ink hover:bg-surface-2`,
} as const;

export function CtaLink({
  href,
  variant = "filled",
  children,
}: {
  href: string;
  variant?: keyof typeof VARIANTS;
  children: React.ReactNode;
}) {
  return (
    <Link href={href} className={VARIANTS[variant]}>
      {children}
    </Link>
  );
}
```

`whitespace-nowrap` memenuhi aturan §13 master spec: label CTA tidak pernah membungkus ke baris kedua di desktop.

- [ ] **Step 4: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/components/ui/cta-link.test.tsx`
Expected: PASS, tiga tes hijau.

- [ ] **Step 5: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos.

- [ ] **Step 6: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/components/ui/cta-link.tsx dml-web/src/components/ui/cta-link.test.tsx
git commit -m "feat: primitif CtaLink, dipakai ulang seluruh CTA primer beranda"
```

---

### Task 7: Seksi 1, Hero pinned orbit malam

**Files:**
- Create: `dml-web/src/features/home/sequence/night-sequence.tsx`
- Create: `dml-web/src/features/home/hero.tsx`
- Test: `dml-web/src/features/home/hero.test.tsx`

**Interfaces:**
- Consumes: `MEDIA["hero-malam"]`, `avifSrc`, `avifSrcSet` dari Task 4; `registerGsap`, `gsap`, `ScrollTrigger`, `SplitText` dari `@/lib/motion/gsap`; `usePrefersReducedMotion` dari `@/lib/motion/use-prefers-reduced-motion`; `CtaLink` dari Task 6
- Produces: `<Hero />` default export, Server Component. Task 16 mengimpor dan merender ini sebagai seksi pertama `page.tsx`.

- [ ] **Step 1: Tulis tes yang gagal untuk konten server**

Create `dml-web/src/features/home/hero.test.tsx`:

```tsx
import { describe, expect, it } from "vitest";
import { render, screen } from "@testing-library/react";
import { Hero } from "./hero";

describe("Hero", () => {
  it("render headline sebagai h1 dan CTA ke /kontak", () => {
    render(<Hero />);
    expect(screen.getByRole("heading", { level: 1 })).toBeInTheDocument();
    expect(screen.getByRole("link", { name: /hubungi kami/i })).toHaveAttribute("href", "/kontak");
  });

  it("gambar poster frame tengah punya priority dan alt text", () => {
    render(<Hero />);
    const poster = screen.getByAltText(/orbit malam dua kapal sts, posisi tengah orbit/i);
    expect(poster).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/features/home/hero.test.tsx`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 3: Implementasi seksi (Server Component)**

Create `dml-web/src/features/home/hero.tsx`:

```tsx
import Image from "next/image";
import { CtaLink } from "@/components/ui/cta-link";
import { MEDIA, avifSrc } from "@/lib/media/manifest";
import { NightSequence } from "./sequence/night-sequence";

export function Hero() {
  const frames = MEDIA["hero-malam"];
  const posterFrame = frames[4];
  if (!posterFrame) {
    throw new Error("MEDIA['hero-malam'] harus punya minimal 5 frame untuk poster tengah");
  }

  return (
    <section className="relative flex min-h-[100dvh] items-end overflow-hidden pt-24 pb-16 md:pb-24">
      <Image
        src={avifSrc(posterFrame, 1600)}
        alt={posterFrame.alt}
        fill
        priority
        sizes="100vw"
        className="object-cover"
      />
      <NightSequence frames={frames} />
      <div className="relative z-10 mx-auto w-full max-w-[1400px] px-4 md:px-8">
        <h1 className="max-w-[16ch] font-display text-4xl font-bold tracking-tight text-ink md:text-6xl">
          Menggerakkan energi dan orang di perairan Kalimantan sejak 1985.
        </h1>
        <p className="mt-4 max-w-[45ch] text-ink-muted">
          Armada BBM, penyeberangan ro-ro, dan galangan kapal dalam satu grup pelayaran Banjarmasin.
        </p>
        <div className="mt-8">
          {/* TODO(plan-bisnis): arahkan ke /bisnis/transportasi-bbm/permintaan-informasi setelah halaman itu dibangun */}
          <CtaLink href="/kontak">Hubungi Kami</CtaLink>
        </div>
      </div>
    </section>
  );
}
```

Subteks "Armada BBM, penyeberangan ro-ro, dan galangan kapal dalam satu grup pelayaran Banjarmasin." adalah 12 kata, di bawah batas 20 kata §7.1 master spec. Headline dua baris di layar sempit lewat `max-w-[16ch]`, memenuhi disiplin hero maksimal dua baris.

- [ ] **Step 4: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/features/home/hero.test.tsx`
Expected: PASS, dua tes hijau.

- [ ] **Step 5: Implementasi sequence scrubber (client leaf)**

Create `dml-web/src/features/home/sequence/night-sequence.tsx`:

```tsx
"use client";

import { useEffect, useRef } from "react";
import { gsap, registerGsap, ScrollTrigger } from "@/lib/motion/gsap";
import { usePrefersReducedMotion } from "@/lib/motion/use-prefers-reduced-motion";
import { type MediaAsset, avifSrc } from "@/lib/media/manifest";

/**
 * Layer poster (di Hero, Server Component) tetap jadi LCP. Komponen ini
 * menambah sembilan frame sisanya sebagai layer <img> bertumpuk yang
 * di-crossfade lewat opacity mengikuti progress scroll. Tidak ada WebGL,
 * tidak ada elemen di atas fold selain poster yang sudah ada.
 */
export function NightSequence({ frames }: { frames: MediaAsset[] }) {
  const sectionRef = useRef<HTMLElement | null>(null);
  const layerRefs = useRef<(HTMLImageElement | null)[]>([]);
  const reduced = usePrefersReducedMotion();

  useEffect(() => {
    const section = sectionRef.current?.closest("section");
    if (reduced || !section) return;
    registerGsap();

    const ctx = gsap.context(() => {
      const layers = layerRefs.current.filter((el): el is HTMLImageElement => el !== null);
      gsap.set(layers, { opacity: 0 });

      ScrollTrigger.create({
        trigger: section,
        start: "top top",
        end: "+=120%",
        pin: true,
        scrub: 1,
        onUpdate: (self) => {
          const position = self.progress * (layers.length - 1);
          const index = Math.floor(position);
          const localProgress = position - index;
          const current = layers[index];
          const next = layers[index + 1];
          layers.forEach((layer) => gsap.set(layer, { opacity: 0 }));
          if (current) gsap.set(current, { opacity: 1 - localProgress });
          if (next) gsap.set(next, { opacity: localProgress });
        },
      });
    }, sectionRef);

    return () => ctx.revert();
  }, [reduced]);

  if (reduced) return null;

  return (
    <div ref={sectionRef} className="absolute inset-0" aria-hidden>
      {frames.map((frame, index) => (
        <img
          key={frame.id}
          ref={(el) => {
            layerRefs.current[index] = el;
          }}
          src={avifSrc(frame, 1600)}
          alt=""
          className="absolute inset-0 h-full w-full object-cover"
          loading={index === 0 ? "eager" : "lazy"}
        />
      ))}
    </div>
  );
}
```

`aria-hidden` pada wrapper karena layer ini murni dekoratif di atas poster yang sudah punya alt text bermakna; layer tidak menyampaikan informasi baru, hanya animasi. Panjang pin `+=120%` (1,2 layar) sesuai §7.1. `sectionRef.current?.closest("section")` dipakai karena komponen ini dirender di dalam `<section>` milik `Hero` (Server Component), bukan membungkusnya sendiri, supaya `Hero` tetap satu-satunya pemilik struktur DOM section.

- [ ] **Step 6: Checkpoint visual wajib**

Run: `cd dml-web && bun run build && bun run start &` lalu buka `http://localhost:3000` di browser (lewat Playwright headless kalau tidak ada akses browser interaktif) dan scroll pelan-pelan lewat hero.

Kalau crossfade masih terbaca melangkah (bukan gerakan mulus), turunkan jumlah frame aktif dengan mengubah `MEDIA["hero-malam"]` di Task 4 jadi 5 still dan pakai potongan keras (`gsap.set` langsung ke 0/1 tanpa interpolasi `localProgress`) per beat, bukan menambah frame baru. Catat keputusan ini di laporan task kalau terjadi. Matikan server (`kill %1` atau sesuai job control shell) setelah checkpoint selesai.

- [ ] **Step 7: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos.

- [ ] **Step 8: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/features/home/hero.tsx dml-web/src/features/home/hero.test.tsx dml-web/src/features/home/sequence
git commit -m "feat: seksi 1 beranda, hero pinned orbit malam dengan crossfade AVIF"
```

---

### Task 8: Seksi 2, potong keras ke siang

**Files:**
- Create: `dml-web/src/features/home/day-cut.tsx`
- Test: `dml-web/src/features/home/day-cut.test.tsx`

**Interfaces:**
- Consumes: `MEDIA["hari"]`, `avifSrc` dari Task 4; `Reveal` dari `@/components/motion/reveal`
- Produces: `<DayCut />` default export. Task 16 merender sebagai seksi kedua.

- [ ] **Step 1: Tulis tes yang gagal**

Create `dml-web/src/features/home/day-cut.test.tsx`:

```tsx
import { describe, expect, it } from "vitest";
import { render, screen } from "@testing-library/react";
import { DayCut } from "./day-cut";

describe("DayCut", () => {
  it("render paragraf penjelasan ship-to-ship transfer", () => {
    render(<DayCut />);
    expect(screen.getByText(/ship-to-ship/i)).toBeInTheDocument();
  });

  it("render gambar wide anchorage dengan alt text", () => {
    render(<DayCut />);
    expect(screen.getByAltText(/area labuh jangkar armada/i)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/features/home/day-cut.test.tsx`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 3: Implementasi**

Create `dml-web/src/features/home/day-cut.tsx`:

```tsx
import Image from "next/image";
import { MEDIA, avifSrc } from "@/lib/media/manifest";

export function DayCut() {
  const frame = MEDIA["hari"][0];
  if (!frame) {
    throw new Error("MEDIA['hari'] harus punya minimal 1 frame");
  }

  return (
    <section className="relative flex min-h-[80vh] items-center overflow-hidden bg-surface-2">
      <Image
        src={avifSrc(frame, 2400)}
        alt={frame.alt}
        fill
        sizes="100vw"
        className="object-cover opacity-70"
      />
      <div className="relative z-10 mx-auto max-w-[1400px] px-4 md:px-8">
        <p className="max-w-[55ch] text-lg leading-relaxed text-ink md:text-xl">
          Ship-to-ship transfer memindahkan BBM langsung antar kapal di tengah perairan,
          tanpa menunggu antrean sandar pelabuhan. Bagi distribusi bahan bakar di Kalimantan,
          ini yang membuat pasokan sampai tepat waktu ke titik yang sulit dijangkau jetty
          konvensional.
        </p>
      </div>
    </section>
  );
}
```

Parallax lambat (§7.2) didelegasikan ke CSS `background-attachment` atau ditambahkan lewat client leaf terpisah kalau checkpoint visual di Step 5 menunjukkan versi statis ini terasa datar; per Global Constraint motion adalah progressive enhancement, versi tanpa parallax ini sudah benar dan lengkap secara konten, jadi parallax opsional murni kosmetik boleh ditambah tapi tidak wajib untuk task ini lolos.

- [ ] **Step 4: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/features/home/day-cut.test.tsx`
Expected: PASS, dua tes hijau.

- [ ] **Step 5: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos.

- [ ] **Step 6: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/features/home/day-cut.tsx dml-web/src/features/home/day-cut.test.tsx
git commit -m "feat: seksi 2 beranda, potongan siang dengan penjelasan STS"
```

---

### Task 9: Seksi 3, tiga lini bisnis

**Files:**
- Create: `dml-web/src/features/home/business-lines.tsx`
- Test: `dml-web/src/features/home/business-lines.test.tsx`

**Interfaces:**
- Consumes: `MEDIA["lini-bisnis"]`, `avifSrc` dari Task 4; `registerGsap`, `gsap`, `ScrollTrigger` dari `@/lib/motion/gsap`; `usePrefersReducedMotion`; `CtaLink` dari Task 6
- Produces: `<BusinessLines />` default export. Task 16 merender sebagai seksi ketiga.

- [ ] **Step 1: Tulis tes yang gagal**

Create `dml-web/src/features/home/business-lines.test.tsx`:

```tsx
import { describe, expect, it } from "vitest";
import { render, screen } from "@testing-library/react";
import { BusinessLines } from "./business-lines";

describe("BusinessLines", () => {
  it("render tiga kartu lini bisnis dengan judul", () => {
    render(<BusinessLines />);
    expect(screen.getByText("Transportasi BBM")).toBeInTheDocument();
    expect(screen.getByText("Penumpang Ro-Ro")).toBeInTheDocument();
    expect(screen.getByText("Galangan Kapal")).toBeInTheDocument();
  });

  it("kartu tidak punya link ke /bisnis", () => {
    render(<BusinessLines />);
    const links = screen.queryAllByRole("link");
    for (const link of links) {
      expect(link.getAttribute("href")).not.toMatch(/^\/bisnis/);
    }
  });

  it("CTA di seksi ini mengarah ke /kontak", () => {
    render(<BusinessLines />);
    expect(screen.getByRole("link", { name: /hubungi kami/i })).toHaveAttribute("href", "/kontak");
  });
});
```

- [ ] **Step 2: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/features/home/business-lines.test.tsx`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 3: Implementasi**

Create `dml-web/src/features/home/business-lines.tsx`:

```tsx
"use client";

import { useEffect, useRef } from "react";
import Image from "next/image";
import { gsap, registerGsap, ScrollTrigger } from "@/lib/motion/gsap";
import { usePrefersReducedMotion } from "@/lib/motion/use-prefers-reduced-motion";
import { MEDIA, avifSrc } from "@/lib/media/manifest";
import { CtaLink } from "@/components/ui/cta-link";

const CARDS = [
  {
    title: "Transportasi BBM",
    description: "Motor tanker, oil barge, dan SPOB melayani kontrak jangka panjang di hampir seluruh wilayah Kalimantan, standar ISM Code dan ISPS Code.",
    mediaId: "transportasi-bbm",
  },
  {
    title: "Penumpang Ro-Ro",
    description: "Armada KMP Jambo menghubungkan Ketapang, Lembar, Tanjung Perak Surabaya, dan Kumai dengan kabin ber-AC, kafe, dan fasilitas medis.",
    mediaId: "penumpang-roro",
  },
  {
    title: "Galangan Kapal",
    description: "PT Dutabahari Menara Line Dockyard menyediakan bangun kapal baru, konversi, perawatan, dan perbaikan komponen di fasilitas Pasir Mas.",
    mediaId: "galangan-kapal",
  },
] as const;

export function BusinessLines() {
  const sectionRef = useRef<HTMLElement | null>(null);
  const cardRefs = useRef<(HTMLDivElement | null)[]>([]);
  const reduced = usePrefersReducedMotion();

  useEffect(() => {
    const section = sectionRef.current;
    if (reduced || !section) return;
    registerGsap();

    const ctx = gsap.context(() => {
      const cards = cardRefs.current.filter((el): el is HTMLDivElement => el !== null);
      cards.slice(0, -1).forEach((card, index) => {
        const next = cards[index + 1];
        if (!next) return;
        ScrollTrigger.create({
          trigger: next,
          start: "top top",
          end: "bottom top",
          scrub: true,
          onUpdate: (self) => {
            gsap.set(card, {
              scale: 1 - self.progress * 0.06,
              opacity: 1 - self.progress * 0.4,
            });
          },
        });
      });
    }, sectionRef);

    return () => ctx.revert();
  }, [reduced]);

  return (
    <section ref={sectionRef} className="relative">
      {CARDS.map((card, index) => {
        const media = MEDIA["lini-bisnis"].find((asset) => asset.id === card.mediaId);
        if (!media) return null;
        return (
          <div
            key={card.title}
            ref={(el) => {
              cardRefs.current[index] = el;
            }}
            className="sticky top-0 flex min-h-screen items-center bg-surface"
            style={{ zIndex: index + 1 }}
          >
            <div className="relative h-full w-full overflow-hidden">
              <Image src={avifSrc(media, 2400)} alt={media.alt} fill sizes="100vw" className="object-cover" />
              <div className="absolute inset-0 bg-gradient-to-t from-surface via-surface/40 to-transparent" />
              <div className="relative z-10 flex h-full max-w-[1400px] flex-col justify-end px-4 py-16 md:mx-auto md:px-8">
                <h2 className="font-display text-3xl font-bold text-ink md:text-5xl">{card.title}</h2>
                <p className="mt-4 max-w-[55ch] text-ink-muted">{card.description}</p>
                {index === CARDS.length - 1 && (
                  <div className="mt-8">
                    {/* TODO(plan-bisnis): arahkan ke /bisnis/transportasi-bbm/permintaan-informasi setelah halaman itu dibangun */}
                    <CtaLink href="/kontak">Hubungi Kami</CtaLink>
                  </div>
                )}
              </div>
            </div>
          </div>
        );
      })}
    </section>
  );
}
```

Kartu sengaja tanpa `href` per Global Constraint (halaman `/bisnis/<slug>` belum ada). Ini client component penuh (bukan Server Component dengan client leaf terisolasi) karena struktur sticky-stack-nya sendiri butuh ref per kartu untuk ScrollTrigger; ini konsisten dengan §4.2 master spec selama teks tetap ada di markup yang sama (tidak dikirim terpisah dari client), yang benar di sini karena `CARDS` adalah konstanta modul, bukan data yang di-fetch di client.

- [ ] **Step 4: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/features/home/business-lines.test.tsx`
Expected: PASS, tiga tes hijau.

- [ ] **Step 5: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos.

- [ ] **Step 6: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/features/home/business-lines.tsx dml-web/src/features/home/business-lines.test.tsx
git commit -m "feat: seksi 3 beranda, tiga lini bisnis sticky-stack tanpa link /bisnis"
```

---

### Task 10: Dependency three.js dan boundary R3F

**Files:**
- Modify: `dml-web/package.json`

**Interfaces:**
- Consumes: tidak ada
- Produces: `three`, `@react-three/fiber`, `@react-three/drei` tersedia sebagai dependency. Task 11 mengimpor ketiganya langsung; Task 12 hanya memuatnya secara tidak langsung lewat `next/dynamic` ke `fleet-canvas.tsx`, tidak mengimpor `three`/R3F sendiri.

- [ ] **Step 1: Pasang dependency**

Run: `cd dml-web && bun add three @react-three/fiber @react-three/drei && bun add -d @types/three`

- [ ] **Step 2: Verifikasi versi kompatibel React 19**

Run: `cd dml-web && bunx tsc --noEmit`
Expected: PASS. `@react-three/fiber` versi terbaru mendukung React 19; kalau tsc gagal karena mismatch tipe React, catat versi persis yang terpasang (`cat package.json | grep react-three`) di laporan task sebelum lanjut ke Task 11.

- [ ] **Step 3: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos, bundle belum membesar signifikan karena belum ada kode yang mengimpor paket baru ini.

- [ ] **Step 4: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/package.json dml-web/bun.lock
git commit -m "chore: pasang three, @react-three/fiber, @react-three/drei"
```

---

### Task 11: Seksi 4a, canvas 3D fleet comparator

**Files:**
- Create: `dml-web/src/features/home/fleet-3d/hull-geometry.ts`
- Create: `dml-web/src/features/home/fleet-3d/fleet-canvas.tsx`
- Test: `dml-web/src/features/home/fleet-3d/hull-geometry.test.ts`

**Interfaces:**
- Consumes: `FLEET_CLASSES` dari Task 5
- Produces: `buildHullShape(spec: FleetClass): THREE.Shape`, `buildHullGeometry`, `buildSuperstructureGeometry` dari `hull-geometry.ts`. `<FleetCanvas progressRef={React.RefObject<number>} />` client leaf dari `fleet-canvas.tsx`. Task 12 dinamis-mengimpor `FleetCanvas` lewat `next/dynamic` di dalam `fleet-comparator.tsx` yang task itu buat, dan menulis `progressRef` lewat ScrollTrigger. Task 12 (blueprint mobile, tabel a11y) tidak mengimpor apa pun dari file di task ini selain lewat dynamic import tadi, keduanya membaca `FLEET_CLASSES` langsung.

**Interpretasi "morph antar geometri" untuk komponen ini:** bukan interpolasi vertex-level (kelas kapal secara fisik punya bentuk superstruktur berbeda, memaksa morph vertex akan menghasilkan bentuk transisi yang tidak bermakna). Interpretasinya adalah crossfade antara dua mesh diskret, kelas aktif dan kelas berikutnya, digerakkan progress scroll, bahasa visual yang sama dengan crossfade sekuens hero Task 7. Ini keputusan desain eksplisit, dicatat di sini supaya tidak dibaca sebagai penyederhanaan diam-diam dari brief.

- [ ] **Step 1: Tulis tes yang gagal untuk builder geometri**

Create `dml-web/src/features/home/fleet-3d/hull-geometry.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import * as THREE from "three";
import { buildHullShape } from "./hull-geometry";
import { FLEET_CLASSES } from "@/content/fleet";

describe("buildHullShape", () => {
  it("menghasilkan THREE.Shape untuk setiap kelas armada", () => {
    for (const fleetClass of FLEET_CLASSES) {
      const shape = buildHullShape(fleetClass);
      expect(shape).toBeInstanceOf(THREE.Shape);
    }
  });

  it("bounding box shape sebanding dengan lengthMeters kelas", () => {
    const tanker = FLEET_CLASSES.find((entry) => entry.slug === "motor-tanker");
    const tug = FLEET_CLASSES.find((entry) => entry.slug === "tugboat");
    expect(tanker).toBeDefined();
    expect(tug).toBeDefined();
    if (!tanker || !tug) return;

    const tankerShape = buildHullShape(tanker);
    const tugShape = buildHullShape(tug);
    const tankerBox = new THREE.Box2().setFromPoints(tankerShape.getPoints());
    const tugBox = new THREE.Box2().setFromPoints(tugShape.getPoints());
    const tankerLength = tankerBox.max.x - tankerBox.min.x;
    const tugLength = tugBox.max.x - tugBox.min.x;
    expect(tankerLength).toBeGreaterThan(tugLength);
  });
});
```

- [ ] **Step 2: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/features/home/fleet-3d/hull-geometry.test.ts`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 3: Implementasi builder geometri**

Create `dml-web/src/features/home/fleet-3d/hull-geometry.ts`:

```ts
import * as THREE from "three";
import type { FleetClass } from "@/content/types";

/**
 * Profil lambung disederhanakan jadi enam titik kontrol: haluan lancip,
 * bahu, badan paralel, buritan. Diskalakan dari lengthMeters dan
 * beamMeters kelas kapal, dibagi 10 supaya unit dunia three.js tetap
 * kecil (skala meter jadi dm, memudahkan kamera default tanpa near/far
 * ekstrem).
 */
export function buildHullShape(fleetClass: FleetClass): THREE.Shape {
  const length = fleetClass.lengthMeters / 10;
  const halfBeam = fleetClass.beamMeters / 20;

  const shape = new THREE.Shape();
  shape.moveTo(0, 0);
  shape.lineTo(length * 0.08, halfBeam * 0.6);
  shape.lineTo(length * 0.2, halfBeam);
  shape.lineTo(length * 0.85, halfBeam);
  shape.lineTo(length, halfBeam * 0.3);
  shape.lineTo(length * 0.85, -halfBeam);
  shape.lineTo(length * 0.2, -halfBeam);
  shape.lineTo(length * 0.08, -halfBeam * 0.6);
  shape.closePath();

  return shape;
}

export function buildHullGeometry(fleetClass: FleetClass): THREE.ExtrudeGeometry {
  const shape = buildHullShape(fleetClass);
  const depth = Math.max(fleetClass.beamMeters / 40, 0.3);
  const geometry = new THREE.ExtrudeGeometry(shape, { depth, bevelEnabled: false, steps: 1 });
  geometry.rotateX(Math.PI / 2);
  geometry.center();
  return geometry;
}

export function buildSuperstructureGeometry(fleetClass: FleetClass): THREE.BoxGeometry {
  const length = fleetClass.lengthMeters / 10;
  const beam = fleetClass.beamMeters / 10;
  const superstructureLength = fleetClass.slug === "ro-ro-ferry" ? length * 0.6 : length * 0.15;
  const height = fleetClass.slug === "ro-ro-ferry" ? beam * 0.5 : beam * 0.35;
  return new THREE.BoxGeometry(superstructureLength, height, beam * 0.5);
}
```

- [ ] **Step 4: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/features/home/fleet-3d/hull-geometry.test.ts`
Expected: PASS, dua tes hijau.

- [ ] **Step 5: Implementasi canvas R3F (client leaf, satu-satunya isi tree R3F)**

Create `dml-web/src/features/home/fleet-3d/fleet-canvas.tsx`:

```tsx
"use client";

import { useMemo, useRef } from "react";
import { Canvas, useFrame } from "@react-three/fiber";
import { OrbitControls } from "@react-three/drei";
import * as THREE from "three";
import { FLEET_CLASSES } from "@/content/fleet";
import { buildHullGeometry, buildSuperstructureGeometry } from "./hull-geometry";

const WIREFRAME_COLOR = "#FF5A1F";
const HULL_COLOR = "#18292F";

function ClassMesh({ index, opacityRef }: { index: number; opacityRef: React.RefObject<number[]> }) {
  const fleetClass = FLEET_CLASSES[index];
  const materialRef = useRef<THREE.MeshBasicMaterial>(null);
  const wireMaterialRef = useRef<THREE.LineBasicMaterial>(null);

  const hullGeometry = useMemo(() => (fleetClass ? buildHullGeometry(fleetClass) : null), [fleetClass]);
  const superGeometry = useMemo(() => (fleetClass ? buildSuperstructureGeometry(fleetClass) : null), [fleetClass]);

  useFrame(() => {
    const opacity = opacityRef.current[index] ?? 0;
    if (materialRef.current) materialRef.current.opacity = opacity;
    if (wireMaterialRef.current) wireMaterialRef.current.opacity = opacity;
  });

  if (!fleetClass || !hullGeometry || !superGeometry) return null;

  const edges = new THREE.EdgesGeometry(hullGeometry);

  return (
    <group>
      <mesh geometry={hullGeometry}>
        <meshBasicMaterial ref={materialRef} color={HULL_COLOR} transparent opacity={0} />
      </mesh>
      <lineSegments geometry={edges}>
        <lineBasicMaterial ref={wireMaterialRef} color={WIREFRAME_COLOR} transparent opacity={0} />
      </lineSegments>
      <mesh geometry={superGeometry} position={[0, 0.6, 0]}>
        <meshBasicMaterial color={HULL_COLOR} transparent opacity={0} />
      </mesh>
    </group>
  );
}

function Rig({ progressRef }: { progressRef: React.RefObject<number> }) {
  const groupRef = useRef<THREE.Group>(null);
  const opacityRef = useRef<number[]>(FLEET_CLASSES.map(() => 0));

  useFrame((_, delta) => {
    if (groupRef.current) {
      groupRef.current.rotation.y += delta * 0.15;
    }

    const progress = progressRef.current ?? 0;
    const position = progress * (FLEET_CLASSES.length - 1);
    const activeIndex = Math.min(Math.floor(position), FLEET_CLASSES.length - 1);
    const localProgress = position - activeIndex;

    opacityRef.current = FLEET_CLASSES.map((_, index) => {
      if (index === activeIndex) return 1 - localProgress;
      if (index === activeIndex + 1) return localProgress;
      return 0;
    });
  });

  return (
    <group ref={groupRef}>
      {FLEET_CLASSES.map((fleetClass, index) => (
        <ClassMesh key={fleetClass.slug} index={index} opacityRef={opacityRef} />
      ))}
    </group>
  );
}

export function FleetCanvas({ progressRef }: { progressRef: React.RefObject<number> }) {
  return (
    <Canvas camera={{ position: [4, 2, 4], fov: 45 }} dpr={[1, 1.5]}>
      <ambientLight intensity={0.6} />
      <directionalLight position={[3, 5, 2]} intensity={0.8} />
      <Rig progressRef={progressRef} />
      <OrbitControls enableZoom={false} enablePan={false} autoRotate={false} />
    </Canvas>
  );
}
```

`opacityRef` (bukan React state) yang dibaca tiap `useFrame` untuk crossfade, ditulis juga di dalam `useFrame` yang sama (`Rig`), keduanya di dalam boundary R3F. `progressRef` satu-satunya jembatan dari luar canvas: Task 12 menulisnya lewat ScrollTrigger di `fleet-comparator.tsx`, komponen di sini hanya membaca, tidak pernah sebaliknya. `FleetCanvas` belum dipakai komponen manapun sampai Task 12 memuatnya lewat `next/dynamic`, itu wajar untuk client leaf yang sengaja diisolasi lewat dynamic import, bukan tanda ada yang salah.

- [ ] **Step 6: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos. `fleet-canvas.tsx` belum diimpor `page.tsx` manapun di titik ini (itu terjadi di Task 12), tapi tetap ikut type-checked dan lint karena masuk cakupan `tsconfig.json`, dan build tidak gagal karena isinya hanya bergantung pada modul yang sudah ada (`three`, `@react-three/fiber`, `@react-three/drei`, `FLEET_CLASSES` dari Task 5).

- [ ] **Step 7: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/features/home/fleet-3d
git commit -m "feat: seksi 4 beranda, builder geometri dan canvas 3D fleet comparator"
```

---

### Task 12: Seksi 4b, fallback blueprint SVG, tabel spesifikasi a11y, dan rakit fleet comparator

**Files:**
- Create: `dml-web/src/features/fleet/blueprint-svg.tsx`
- Create: `dml-web/src/features/fleet/spec-table.tsx`
- Create: `dml-web/src/features/home/fleet-comparator.tsx`
- Test: `dml-web/src/features/fleet/blueprint-svg.test.tsx`
- Test: `dml-web/src/features/fleet/spec-table.test.tsx`

**Interfaces:**
- Consumes: `FleetClass` type dari `@/content/types`, `FLEET_CLASSES` dari `@/content/fleet` (Task 5), `FleetCanvas` dari `@/features/home/fleet-3d/fleet-canvas` (Task 11)
- Produces: `<BlueprintSvg fleetClasses={FleetClass[]} />`, `<FleetSpecTable fleetClasses={FleetClass[]} />` dipakai langsung di task ini oleh `<FleetComparator />` (default export `fleet-comparator.tsx`), yang dipakai Task 16 sebagai seksi keempat `page.tsx`.

- [ ] **Step 1: Tulis tes yang gagal untuk tabel**

Create `dml-web/src/features/fleet/spec-table.test.tsx`:

```tsx
import { describe, expect, it } from "vitest";
import { render, screen } from "@testing-library/react";
import { FleetSpecTable } from "./spec-table";
import { FLEET_CLASSES } from "@/content/fleet";

describe("FleetSpecTable", () => {
  it("render sebagai table dengan satu baris per kelas kapal", () => {
    render(<FleetSpecTable fleetClasses={FLEET_CLASSES} />);
    const table = screen.getByRole("table");
    const rows = table.querySelectorAll("tbody tr");
    expect(rows).toHaveLength(FLEET_CLASSES.length);
  });

  it("kolom DWT menampilkan tanda hubung untuk kelas tanpa DWT", () => {
    render(<FleetSpecTable fleetClasses={FLEET_CLASSES} />);
    const ferryRow = screen.getByText("Ro-Ro Ferry (KMP Jambo X)").closest("tr");
    expect(ferryRow?.textContent).toContain("-");
  });
});
```

- [ ] **Step 2: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/features/fleet/spec-table.test.tsx`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 3: Implementasi tabel**

Create `dml-web/src/features/fleet/spec-table.tsx`:

```tsx
import type { FleetClass } from "@/content/types";

export function FleetSpecTable({ fleetClasses }: { fleetClasses: FleetClass[] }) {
  return (
    <table className="mt-8 w-full border-collapse text-left font-mono text-sm">
      <caption className="sr-only">Spesifikasi lima kelas kapal armada PT Dutabahari Menara Line</caption>
      <thead>
        <tr className="border-b border-surface-3 text-ink-muted">
          <th scope="col" className="py-3 pr-4 font-normal">Kelas</th>
          <th scope="col" className="py-3 pr-4 font-normal">Panjang</th>
          <th scope="col" className="py-3 pr-4 font-normal">DWT</th>
          <th scope="col" className="py-3 pr-4 font-normal">Kapasitas</th>
          <th scope="col" className="py-3 font-normal">Penumpang</th>
        </tr>
      </thead>
      <tbody>
        {fleetClasses.map((fleetClass) => (
          <tr key={fleetClass.slug} className="border-b border-surface-3/50 text-ink">
            <td className="py-3 pr-4">{fleetClass.name}</td>
            <td className="py-3 pr-4">{fleetClass.lengthMeters} m</td>
            <td className="py-3 pr-4">{fleetClass.dwt ? `${fleetClass.dwt.toLocaleString("id-ID")} DWT` : "-"}</td>
            <td className="py-3 pr-4">{fleetClass.capacityLabel}</td>
            <td className="py-3">{fleetClass.passengerCapacity ? `${fleetClass.passengerCapacity} orang` : "-"}</td>
          </tr>
        ))}
      </tbody>
    </table>
  );
}
```

- [ ] **Step 4: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/features/fleet/spec-table.test.tsx`
Expected: PASS, dua tes hijau.

- [ ] **Step 5: Tulis tes yang gagal untuk blueprint SVG**

Create `dml-web/src/features/fleet/blueprint-svg.test.tsx`:

```tsx
import { describe, expect, it } from "vitest";
import { render, screen } from "@testing-library/react";
import { BlueprintSvg } from "./blueprint-svg";
import { FLEET_CLASSES } from "@/content/fleet";

describe("BlueprintSvg", () => {
  it("render satu svg per kelas kapal dengan title untuk screen reader", () => {
    render(<BlueprintSvg fleetClasses={FLEET_CLASSES} />);
    for (const fleetClass of FLEET_CLASSES) {
      expect(screen.getByText(fleetClass.altText)).toBeInTheDocument();
    }
  });
});
```

- [ ] **Step 6: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/features/fleet/blueprint-svg.test.tsx`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 7: Implementasi blueprint SVG**

Create `dml-web/src/features/fleet/blueprint-svg.tsx`:

```tsx
import type { FleetClass } from "@/content/types";

/**
 * Path haluan-buritan sederhana, ditulis tangan per kelas berdasarkan
 * proporsi panjang:lebar dari FleetClass, bukan diturunkan dari
 * hull-geometry.ts (itu geometri 3D, ini SVG 2D independen untuk mobile,
 * sesuai spec bagian 7.4: tiga representasi yang masing-masing pekerjaan
 * nyata, bukan satu representasi yang diproyeksikan ke dua tempat).
 */
function hullPath(fleetClass: FleetClass): string {
  const length = 200;
  const halfBeam = (fleetClass.beamMeters / fleetClass.lengthMeters) * length * 0.5;
  return `M 0 0 L ${length * 0.08} ${halfBeam * 0.6} L ${length * 0.2} ${halfBeam} L ${length * 0.85} ${halfBeam} L ${length} ${halfBeam * 0.3} L ${length * 0.85} ${-halfBeam} L ${length * 0.2} ${-halfBeam} L ${length * 0.08} ${-halfBeam * 0.6} Z`;
}

export function BlueprintSvg({ fleetClasses }: { fleetClasses: FleetClass[] }) {
  return (
    <div className="grid gap-8 md:grid-cols-2">
      {fleetClasses.map((fleetClass) => (
        <figure key={fleetClass.slug} className="rounded-card border border-surface-3 bg-surface-2 p-6">
          <svg viewBox="-20 -60 240 120" className="h-auto w-full" role="img" aria-labelledby={`blueprint-${fleetClass.slug}`}>
            <title id={`blueprint-${fleetClass.slug}`}>{fleetClass.altText}</title>
            <path d={hullPath(fleetClass)} fill="none" stroke="#FF5A1F" strokeWidth={1.5} />
          </svg>
          <figcaption className="mt-4 text-sm text-ink-muted">
            <span className="text-ink">{fleetClass.name}</span>, {fleetClass.lengthMeters} m, {fleetClass.capacityLabel}
          </figcaption>
        </figure>
      ))}
    </div>
  );
}
```

- [ ] **Step 8: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/features/fleet/blueprint-svg.test.tsx`
Expected: PASS.

- [ ] **Step 9: Implementasi wrapper pin (rakit canvas Task 11 dan fallback di atas)**

Create `dml-web/src/features/home/fleet-comparator.tsx`:

```tsx
"use client";

import dynamic from "next/dynamic";
import { useEffect, useRef, useState } from "react";
import { gsap, registerGsap, ScrollTrigger } from "@/lib/motion/gsap";
import { usePrefersReducedMotion } from "@/lib/motion/use-prefers-reduced-motion";
import { FLEET_CLASSES } from "@/content/fleet";
import { BlueprintSvg } from "@/features/fleet/blueprint-svg";
import { FleetSpecTable } from "@/features/fleet/spec-table";

const FleetCanvas = dynamic(() => import("./fleet-3d/fleet-canvas").then((mod) => mod.FleetCanvas), {
  ssr: false,
});

export function FleetComparator() {
  const sectionRef = useRef<HTMLElement | null>(null);
  const progressRef = useRef(0);
  const reduced = usePrefersReducedMotion();
  const [canvasVisible, setCanvasVisible] = useState(false);
  const [isDesktop, setIsDesktop] = useState(true);

  useEffect(() => {
    const mql = window.matchMedia("(min-width: 768px)");
    setIsDesktop(mql.matches);
    const onChange = () => setIsDesktop(mql.matches);
    mql.addEventListener("change", onChange);
    return () => mql.removeEventListener("change", onChange);
  }, []);

  useEffect(() => {
    const section = sectionRef.current;
    if (!section || !isDesktop) return;

    const observer = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (entry.isIntersecting) setCanvasVisible(true);
        }
      },
      { rootMargin: "200px" },
    );
    observer.observe(section);
    return () => observer.disconnect();
  }, [isDesktop]);

  useEffect(() => {
    const section = sectionRef.current;
    if (reduced || !section || !isDesktop) return;
    registerGsap();

    const ctx = gsap.context(() => {
      ScrollTrigger.create({
        trigger: section,
        start: "top top",
        end: "+=300%",
        pin: true,
        scrub: 1,
        onUpdate: (self) => {
          progressRef.current = self.progress;
        },
      });
    }, sectionRef);

    return () => ctx.revert();
  }, [reduced, isDesktop]);

  return (
    <section ref={sectionRef} className="relative min-h-screen bg-surface py-16 md:py-0">
      <div className="mx-auto max-w-[1400px] px-4 md:px-8">
        <h2 className="font-display text-3xl font-bold text-ink md:text-5xl">Perbandingan Armada</h2>
        <p className="mt-4 max-w-[55ch] text-ink-muted">
          Lima kelas kapal, dari SPOB terkecil sampai motor tanker terbesar, dalam satu skala.
        </p>
      </div>

      {isDesktop && !reduced ? (
        <div className="relative mt-8 h-[60vh] md:h-[70vh]">
          {canvasVisible && <FleetCanvas progressRef={progressRef} />}
        </div>
      ) : (
        <div className="mt-8">
          <BlueprintSvg fleetClasses={FLEET_CLASSES} />
        </div>
      )}

      <div className="mx-auto max-w-[1400px] px-4 md:px-8">
        <FleetSpecTable fleetClasses={FLEET_CLASSES} />
      </div>
    </section>
  );
}
```

Di bawah 768px atau `prefers-reduced-motion: reduce`, canvas 3D tidak pernah dimuat sama sekali, langsung fallback `BlueprintSvg`. Tabel `FleetSpecTable` selalu render di ketiga kondisi (desktop 3D, mobile, reduced motion), memenuhi §13 master spec: informasi armada tidak pernah hanya ada di dalam canvas.

- [ ] **Step 10: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos. `FleetComparator` sekarang punya kedua dependency-nya (canvas dari Task 11, fallback dari Step 1-7 task ini), jadi ini titik pertama seksi 4 lengkap bisa di-build utuh.

- [ ] **Step 11: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/features/fleet dml-web/src/features/home/fleet-comparator.tsx
git commit -m "feat: seksi 4 beranda, fallback blueprint SVG, tabel a11y, dan rakit fleet comparator"
```

---

### Task 13: Seksi 5, peta rute ro-ro

**Files:**
- Create: `dml-web/src/features/route-map/ports.ts`
- Create: `dml-web/src/features/home/route-map.tsx`
- Test: `dml-web/src/features/route-map/ports.test.ts`

**Interfaces:**
- Consumes: `registerGsap`, `gsap`, `ScrollTrigger` dari `@/lib/motion/gsap`; `usePrefersReducedMotion`
- Produces: `<RouteMap />` default export dari `route-map.tsx`. Task 16 merender sebagai seksi kelima. `PORTS: Port[]`, `type Port = { name: string; x: number; y: number }` dari `ports.ts`.

- [ ] **Step 1: Tulis tes yang gagal**

Create `dml-web/src/features/route-map/ports.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { PORTS } from "./ports";

describe("PORTS", () => {
  it("berisi tepat 4 pelabuhan sesuai rute ro-ro", () => {
    expect(PORTS).toHaveLength(4);
    expect(PORTS.map((p) => p.name)).toEqual(["Ketapang", "Lembar", "Tanjung Perak Surabaya", "Kumai"]);
  });
});
```

- [ ] **Step 2: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/features/route-map/ports.test.ts`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 3: Implementasi data pelabuhan**

Create `dml-web/src/features/route-map/ports.ts`:

```ts
export type Port = {
  name: string;
  x: number;
  y: number;
};

/**
 * Koordinat x/y dalam viewBox SVG 400x300, bukan koordinat geografis
 * asli. Posisi relatif disusun tangan supaya keempat pelabuhan terbaca
 * sebagai jaringan Ketapang - Lembar - Tanjung Perak - Kumai, sesuai
 * urutan rute di spec bagian 2, bukan proyeksi peta akurat.
 */
export const PORTS: Port[] = [
  { name: "Ketapang", x: 40, y: 220 },
  { name: "Lembar", x: 110, y: 260 },
  { name: "Tanjung Perak Surabaya", x: 220, y: 180 },
  { name: "Kumai", x: 320, y: 90 },
];
```

- [ ] **Step 4: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/features/route-map/ports.test.ts`
Expected: PASS.

- [ ] **Step 5: Implementasi seksi**

Create `dml-web/src/features/home/route-map.tsx`:

```tsx
"use client";

import { useEffect, useRef } from "react";
import { gsap, registerGsap, ScrollTrigger } from "@/lib/motion/gsap";
import { usePrefersReducedMotion } from "@/lib/motion/use-prefers-reduced-motion";
import { PORTS } from "@/features/route-map/ports";

function routePath(): string {
  return PORTS.map((port, index) => `${index === 0 ? "M" : "L"} ${port.x} ${port.y}`).join(" ");
}

export function RouteMap() {
  const sectionRef = useRef<HTMLElement | null>(null);
  const pathRef = useRef<SVGPathElement | null>(null);
  const reduced = usePrefersReducedMotion();

  useEffect(() => {
    const section = sectionRef.current;
    const path = pathRef.current;
    if (reduced || !section || !path) return;
    registerGsap();

    const ctx = gsap.context(() => {
      const length = path.getTotalLength();
      gsap.set(path, { strokeDasharray: length, strokeDashoffset: length });

      gsap.to(path, {
        strokeDashoffset: 0,
        ease: "none",
        scrollTrigger: {
          trigger: section,
          start: "top 70%",
          end: "bottom 70%",
          scrub: 1,
        },
      });
    }, sectionRef);

    return () => ctx.revert();
  }, [reduced]);

  return (
    <section ref={sectionRef} className="bg-surface-2 py-24">
      <div className="mx-auto max-w-[1400px] px-4 md:px-8">
        <h2 className="font-display text-3xl font-bold text-ink md:text-5xl">Rute Penyeberangan Ro-Ro</h2>
        <p className="mt-4 max-w-[55ch] text-ink-muted">
          Empat pelabuhan, satu jaringan penyeberangan yang menghubungkan Kalimantan dan Jawa.
        </p>
        <svg viewBox="0 0 400 300" className="mt-12 h-auto w-full max-w-2xl" role="img" aria-label="Peta rute penyeberangan Ketapang, Lembar, Tanjung Perak Surabaya, dan Kumai">
          <path ref={pathRef} d={routePath()} fill="none" stroke="#FF5A1F" strokeWidth={2} />
          {PORTS.map((port) => (
            <g key={port.name}>
              <circle cx={port.x} cy={port.y} r={4} fill="#FF5A1F" />
              <text x={port.x + 8} y={port.y + 4} fontSize={11} fill="#F2EFE9" fontFamily="var(--font-mono)">
                {port.name}
              </text>
            </g>
          ))}
        </svg>
      </div>
    </section>
  );
}
```

Ketika `reduced` true, `strokeDasharray`/`strokeDashoffset` tidak pernah diset lewat GSAP, jadi path SVG default tampil penuh (tidak ada `stroke-dasharray` di CSS statis manapun), memenuhi requirement reduced-motion tanpa perlu cabang render terpisah.

- [ ] **Step 6: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos.

- [ ] **Step 7: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/features/route-map dml-web/src/features/home/route-map.tsx
git commit -m "feat: seksi 5 beranda, peta rute ro-ro dengan path draw-on-scroll"
```

---

### Task 14: Seksi 6, silsilah horizontal pan

**Files:**
- Create: `dml-web/src/features/timeline/lineage-pan.tsx`
- Create: `dml-web/src/features/home/lineage.tsx`
- Test: `dml-web/src/features/home/lineage.test.tsx`

**Interfaces:**
- Consumes: `TIMELINE` dari `@/content/timeline` (sudah ada, Plan 2 Task 13); `registerGsap`, `gsap`, `ScrollTrigger`; `usePrefersReducedMotion`
- Produces: `<Lineage />` default export dari `lineage.tsx`. Task 16 merender sebagai seksi keenam.

`TIMELINE` saat ini hanya berisi satu entri (pendirian 1985), karena sumber publik yang tersedia tidak mengonfirmasi tanggal lain (lihat komentar di `src/content/timeline.ts`, ditulis Plan 2 Task 13). Komponen di task ini dibangun generik untuk N entri, bukan di-hardcode untuk satu entri, supaya begitu klien mengonfirmasi tanggal ekspansi lini bisnis dan `TIMELINE` bertambah, section ini otomatis dapat lebih banyak jarak pan tanpa perubahan kode. Dengan satu entri, pan akan terasa pendek/nyaris statis, ini keterbatasan data yang jujur, bukan bug untuk "diperbaiki" dengan menambah tanggal karangan.

- [ ] **Step 1: Tulis tes yang gagal**

Create `dml-web/src/features/home/lineage.test.tsx`:

```tsx
import { describe, expect, it } from "vitest";
import { render, screen } from "@testing-library/react";
import { Lineage } from "./lineage";
import { TIMELINE } from "@/content/timeline";

describe("Lineage", () => {
  it("render satu kartu per entri TIMELINE", () => {
    render(<Lineage />);
    for (const entry of TIMELINE) {
      expect(screen.getByText(String(entry.year))).toBeInTheDocument();
    }
  });

  it("render link ke versi lengkap silsilah", () => {
    render(<Lineage />);
    expect(screen.getByRole("link", { name: /silsilah lengkap/i })).toHaveAttribute(
      "href",
      "/tentang-kami#silsilah",
    );
  });
});
```

- [ ] **Step 2: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/features/home/lineage.test.tsx`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 3: Implementasi client leaf pan**

Create `dml-web/src/features/timeline/lineage-pan.tsx`:

```tsx
"use client";

import { useEffect, useRef } from "react";
import { gsap, registerGsap, ScrollTrigger } from "@/lib/motion/gsap";
import { usePrefersReducedMotion } from "@/lib/motion/use-prefers-reduced-motion";
import type { TimelineEntry } from "@/content/types";

export function LineagePan({ entries }: { entries: TimelineEntry[] }) {
  const wrapperRef = useRef<HTMLDivElement | null>(null);
  const trackRef = useRef<HTMLDivElement | null>(null);
  const reduced = usePrefersReducedMotion();

  useEffect(() => {
    const wrapper = wrapperRef.current;
    const track = trackRef.current;
    if (reduced || !wrapper || !track) return;
    registerGsap();

    const ctx = gsap.context(() => {
      const distance = track.scrollWidth - wrapper.clientWidth;
      if (distance <= 0) return;

      gsap.to(track, {
        x: -distance,
        ease: "none",
        scrollTrigger: {
          trigger: wrapper,
          start: "top top",
          end: `+=${distance}`,
          pin: true,
          scrub: 1,
          invalidateOnRefresh: true,
        },
      });
    }, wrapperRef);

    return () => ctx.revert();
  }, [reduced]);

  return (
    <div ref={wrapperRef} className="overflow-hidden">
      <div ref={trackRef} className={reduced ? "flex flex-wrap gap-6" : "flex gap-6"}>
        {entries.map((entry) => (
          <div key={entry.year} className="w-[min(80vw,420px)] shrink-0 rounded-card border border-surface-3 bg-surface-2 p-8">
            <p className="font-mono text-3xl text-accent">{entry.year}</p>
            <p className="mt-4 text-ink-muted">{entry.label}</p>
          </div>
        ))}
      </div>
    </div>
  );
}
```

Ini satu-satunya scroll hijack horizontal di seluruh situs, per §7.6 master spec. `reduced` mengganti `flex` jadi `flex-wrap`, supaya dengan motion mati kartu tersusun vertikal wajar, bukan tersembunyi di luar viewport tanpa cara scroll horizontal.

- [ ] **Step 4: Implementasi seksi**

Create `dml-web/src/features/home/lineage.tsx`:

```tsx
import Link from "next/link";
import { TIMELINE } from "@/content/timeline";
import { LineagePan } from "@/features/timeline/lineage-pan";

export function Lineage() {
  return (
    <section className="bg-surface py-24">
      <div className="mx-auto max-w-[1400px] px-4 md:px-8">
        <h2 className="font-display text-3xl font-bold text-ink md:text-5xl">Silsilah</h2>
      </div>
      <div className="mt-12">
        <LineagePan entries={TIMELINE} />
      </div>
      <div className="mx-auto mt-8 max-w-[1400px] px-4 md:px-8">
        <Link href="/tentang-kami#silsilah" className="text-sm font-medium text-accent hover:text-accent-hover">
          Lihat silsilah lengkap
        </Link>
      </div>
    </section>
  );
}
```

- [ ] **Step 5: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/features/home/lineage.test.tsx`
Expected: PASS, dua tes hijau.

- [ ] **Step 6: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos.

- [ ] **Step 7: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/features/timeline dml-web/src/features/home/lineage.tsx dml-web/src/features/home/lineage.test.tsx
git commit -m "feat: seksi 6 beranda, silsilah horizontal pan generik untuk N entri"
```

---

### Task 15: Seksi 7, sertifikasi dan angka armada

**Files:**
- Create: `dml-web/src/lib/motion/use-counter.ts`
- Create: `dml-web/src/features/home/certifications.tsx`
- Test: `dml-web/src/lib/motion/use-counter.test.ts`
- Test: `dml-web/src/features/home/certifications.test.tsx`

**Interfaces:**
- Consumes: `COMPANY.certifications`, `COMPANY.fleetSummary` dari `@/content/company`; `Reveal` dari `@/components/motion/reveal`
- Produces: `useCounter(target: number, options?: { duration?: number }): { ref: React.RefObject<HTMLElement>; value: number }` dari `use-counter.ts`. `<Certifications />` default export. Task 16 (assembly) merender ini sebagai seksi ketujuh.

- [ ] **Step 1: Tulis tes yang gagal untuk hook**

Create `dml-web/src/lib/motion/use-counter.test.ts`:

```ts
import { describe, expect, it, vi } from "vitest";
import { renderHook } from "@testing-library/react";
import { useCounter } from "./use-counter";

describe("useCounter", () => {
  it("mulai dari 0 sebelum elemen masuk viewport", () => {
    const { result } = renderHook(() => useCounter(15));
    expect(result.current.value).toBe(0);
  });

  it("mengembalikan ref untuk dipasang ke elemen target", () => {
    const { result } = renderHook(() => useCounter(15));
    expect(result.current.ref).toBeDefined();
  });
});
```

`IntersectionObserver` tidak tersedia di jsdom secara default; kalau `vitest.config.mts` belum menyediakan polyfill global, tambahkan mock minimal di test setup file yang sudah ada (cek `vitest.config.mts` untuk `setupFiles`) sebelum menjalankan tes ini. Jangan mock `IntersectionObserver` di dalam file tes ini sendiri kalau sudah ada setup file global, supaya konsisten dengan test lain yang mungkin juga butuh polyfill yang sama.

- [ ] **Step 2: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/lib/motion/use-counter.test.ts`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 3: Implementasi hook**

Create `dml-web/src/lib/motion/use-counter.ts`:

```ts
"use client";

import { useEffect, useRef, useState } from "react";
import { usePrefersReducedMotion } from "./use-prefers-reduced-motion";

export function useCounter(
  target: number,
  options?: { duration?: number },
): { ref: React.RefObject<HTMLElement | null>; value: number } {
  const ref = useRef<HTMLElement | null>(null);
  const [value, setValue] = useState(0);
  const reduced = usePrefersReducedMotion();
  const duration = options?.duration ?? 1200;

  useEffect(() => {
    const node = ref.current;
    if (!node) return;

    if (reduced) {
      setValue(target);
      return;
    }

    let frame: number | null = null;
    const observer = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (!entry.isIntersecting) continue;
          observer.disconnect();
          const start = performance.now();

          const tick = (now: number) => {
            const elapsed = now - start;
            const progress = Math.min(elapsed / duration, 1);
            setValue(Math.round(target * progress));
            if (progress < 1) {
              frame = requestAnimationFrame(tick);
            }
          };
          frame = requestAnimationFrame(tick);
        }
      },
      { threshold: 0.4 },
    );
    observer.observe(node);

    return () => {
      observer.disconnect();
      if (frame !== null) cancelAnimationFrame(frame);
    };
  }, [target, duration, reduced]);

  return { ref, value };
}
```

- [ ] **Step 4: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/lib/motion/use-counter.test.ts`
Expected: PASS, dua tes hijau.

- [ ] **Step 5: Tulis tes yang gagal untuk seksi**

Create `dml-web/src/features/home/certifications.test.tsx`:

```tsx
import { describe, expect, it } from "vitest";
import { render, screen } from "@testing-library/react";
import { Certifications } from "./certifications";
import { COMPANY } from "@/content/company";

describe("Certifications", () => {
  it("render setiap sertifikasi perusahaan", () => {
    render(<Certifications />);
    for (const cert of COMPANY.certifications) {
      expect(screen.getByText(cert)).toBeInTheDocument();
    }
  });

  it("render label jumlah kapal dan total DWT", () => {
    render(<Certifications />);
    expect(screen.getByText(/kapal/i)).toBeInTheDocument();
    expect(screen.getByText(/dwt/i)).toBeInTheDocument();
  });
});
```

- [ ] **Step 6: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/features/home/certifications.test.tsx`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 7: Implementasi seksi**

Create `dml-web/src/features/home/certifications.tsx`:

```tsx
"use client";

import { COMPANY } from "@/content/company";
import { useCounter } from "@/lib/motion/use-counter";
import { Reveal } from "@/components/motion/reveal";

function VesselCounter() {
  const { ref, value } = useCounter(COMPANY.fleetSummary.vessels);
  return (
    <p ref={ref as React.RefObject<HTMLParagraphElement>} className="font-mono text-5xl text-accent md:text-6xl">
      {value}
    </p>
  );
}

function DwtCounter() {
  const { ref, value } = useCounter(COMPANY.fleetSummary.totalDwt);
  return (
    <p ref={ref as React.RefObject<HTMLParagraphElement>} className="font-mono text-5xl text-accent md:text-6xl">
      {value.toLocaleString("id-ID")}
    </p>
  );
}

export function Certifications() {
  return (
    <section className="bg-surface-2 py-24">
      <div className="mx-auto max-w-[1400px] px-4 md:px-8">
        <div className="grid gap-8 md:grid-cols-2">
          <div>
            <VesselCounter />
            <p className="mt-2 text-ink-muted">
              Kapal {/* unverified: MagicPort */}
            </p>
          </div>
          <div>
            <DwtCounter />
            <p className="mt-2 text-ink-muted">
              Total DWT {/* unverified: MagicPort */}
            </p>
          </div>
        </div>
        <Reveal className="mt-16 flex flex-wrap gap-4" stagger={0.08}>
          {COMPANY.certifications.map((cert) => (
            <span key={cert} className="rounded-full border border-surface-3 px-4 py-2 font-mono text-sm text-ink-muted">
              {cert}
            </span>
          ))}
        </Reveal>
      </div>
    </section>
  );
}
```

- [ ] **Step 8: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/features/home/certifications.test.tsx`
Expected: PASS, dua tes hijau.

- [ ] **Step 9: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos.

- [ ] **Step 10: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/lib/motion/use-counter.ts dml-web/src/lib/motion/use-counter.test.ts dml-web/src/features/home/certifications.tsx dml-web/src/features/home/certifications.test.tsx
git commit -m "feat: seksi 7 beranda, sertifikasi dan counter angka armada"
```

---

### Task 16: Seksi 9, CTA dan rakit beranda

**Files:**
- Create: `dml-web/src/features/home/cta-section.tsx`
- Modify: `dml-web/src/app/(site)/page.tsx`
- Test: `dml-web/src/features/home/cta-section.test.tsx`

**Interfaces:**
- Consumes: `CtaLink` dari Task 6; `Hero`, `DayCut`, `BusinessLines`, `FleetComparator`, `RouteMap`, `Lineage`, `Certifications` dari Task 7-15
- Produces: `<CtaSection />` default export. `page.tsx` merakit seluruh 8 seksi berurutan.

- [ ] **Step 1: Tulis tes yang gagal**

Create `dml-web/src/features/home/cta-section.test.tsx`:

```tsx
import { describe, expect, it } from "vitest";
import { render, screen } from "@testing-library/react";
import { CtaSection } from "./cta-section";

describe("CtaSection", () => {
  it("render satu CTA primer ke /kontak", () => {
    render(<CtaSection />);
    const links = screen.getAllByRole("link", { name: /hubungi kami/i });
    expect(links).toHaveLength(1);
    expect(links[0]).toHaveAttribute("href", "/kontak");
  });
});
```

- [ ] **Step 2: Jalankan tes untuk memastikan gagal**

Run: `cd dml-web && bun run test src/features/home/cta-section.test.tsx`
Expected: FAIL, modul tidak ditemukan.

- [ ] **Step 3: Implementasi**

Create `dml-web/src/features/home/cta-section.tsx`:

```tsx
import { CtaLink } from "@/components/ui/cta-link";

export function CtaSection() {
  return (
    <section className="bg-surface py-24">
      <div className="mx-auto max-w-[1400px] px-4 text-center md:px-8">
        <h2 className="mx-auto max-w-[20ch] font-display text-3xl font-bold text-ink md:text-5xl">
          Siap membahas kebutuhan pengangkutan atau penyeberangan Anda?
        </h2>
        <div className="mt-8 flex justify-center">
          {/* TODO(plan-bisnis): arahkan ke /bisnis/transportasi-bbm/permintaan-informasi setelah halaman itu dibangun */}
          <CtaLink href="/kontak">Hubungi Kami</CtaLink>
        </div>
      </div>
    </section>
  );
}
```

Ini satu-satunya CTA "Hubungi Kami" yang berdiri sendiri di luar konteks kartu lini bisnis; kartu lini bisnis (Task 9) punya CTA sendiri di kartu terakhir untuk alasan naratif (menutup narasi tiga lini sebelum pindah ke seksi lain), keduanya sama-sama mengarah ke `/kontak` dengan intent yang sama (satu intent: "hubungi kami"), jadi tidak melanggar Global Constraint "satu CTA primer per intent", karena constraint itu soal tidak adanya dua *tujuan* CTA yang bersaing (mis. "Hubungi Kami" vs "Pesan Sekarang" untuk hal yang sama), bukan soal jumlah kemunculan tombol dengan intent yang identik di halaman panjang.

Footer global sudah dirender oleh `(site)/layout.tsx` (Plan 1/2, `SiteFooter`), section ini tidak membangun ulang footer.

- [ ] **Step 4: Jalankan tes untuk memastikan lolos**

Run: `cd dml-web && bun run test src/features/home/cta-section.test.tsx`
Expected: PASS.

- [ ] **Step 5: Rakit page.tsx**

Modify `dml-web/src/app/(site)/page.tsx`, ganti seluruh isi placeholder Plan 1:

```tsx
import { Hero } from "@/features/home/hero";
import { DayCut } from "@/features/home/day-cut";
import { BusinessLines } from "@/features/home/business-lines";
import { FleetComparator } from "@/features/home/fleet-comparator";
import { RouteMap } from "@/features/home/route-map";
import { Lineage } from "@/features/home/lineage";
import { Certifications } from "@/features/home/certifications";
import { CtaSection } from "@/features/home/cta-section";

export default function HomePage() {
  return (
    <>
      <Hero />
      <DayCut />
      <BusinessLines />
      <FleetComparator />
      <RouteMap />
      <Lineage />
      <Certifications />
      <CtaSection />
    </>
  );
}
```

Metadata halaman ini sudah ditangani `(site)/layout.tsx` untuk path `/` (Plan 2, `buildMetadata` dengan `path: "/"`), tidak perlu `export const metadata` terpisah di file ini. JSON-LD `Organization` juga sudah global lewat layout yang sama; beranda tidak butuh `breadcrumbJsonLd` sendiri karena ini root, bukan halaman dalam.

- [ ] **Step 6: Gerbang kualitas**

Run: `cd dml-web && bun run lint && bunx tsc --noEmit && bun run test && bun run build`
Expected: semua lolos, `bun run build` menghasilkan halaman `/` statis atau server-rendered tanpa error.

- [ ] **Step 7: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/src/features/home/cta-section.tsx dml-web/src/features/home/cta-section.test.tsx dml-web/src/app/\(site\)/page.tsx
git commit -m "feat: seksi 9 beranda dan rakit 8 seksi ke page.tsx"
```

---

### Task 17: Testing dan gerbang akhir

**Files:**
- Create: `dml-web/tests/e2e/beranda.spec.ts`
- Modify: `dml-web/tests/e2e/no-js.spec.ts`
- Create: `dml-web/lighthouserc.json`
- Modify: `dml-web/package.json` (script `lighthouse`, tambah ke `check`)

**Interfaces:**
- Consumes: seluruh seksi Task 7-16 sudah terpasang di `(site)/page.tsx`
- Produces: tidak ada API baru, ini gerbang verifikasi akhir plan.

- [ ] **Step 1: Tulis spec beranda dengan reduced motion**

Create `dml-web/tests/e2e/beranda.spec.ts`:

```ts
import { test, expect } from "@playwright/test";

test.describe("beranda dengan prefers-reduced-motion: reduce", () => {
  test.use({ reducedMotion: "reduce" });

  test("seluruh 8 seksi tampil penuh tanpa motion", async ({ page }) => {
    await page.goto("/");

    await expect(page.getByRole("heading", { level: 1 })).toBeVisible();
    await expect(page.getByRole("heading", { name: "Perbandingan Armada" })).toBeVisible();
    await expect(page.getByRole("heading", { name: "Rute Penyeberangan Ro-Ro" })).toBeVisible();
    await expect(page.getByRole("heading", { name: "Silsilah" })).toBeVisible();
    await expect(page.getByRole("table")).toBeVisible();

    const htmlClass = await page.locator("html").getAttribute("class");
    expect(htmlClass).not.toContain("lenis");
  });

  test("canvas 3D tidak dimuat saat reduced motion", async ({ page }) => {
    await page.goto("/");
    await expect(page.locator("canvas")).toHaveCount(0);
  });
});

test.describe("beranda mobile", () => {
  test.use({ viewport: { width: 375, height: 812 } });

  test("fallback blueprint SVG tampil, bukan canvas", async ({ page }) => {
    await page.goto("/");
    await expect(page.locator("canvas")).toHaveCount(0);
    await expect(page.locator("svg[role='img']").first()).toBeVisible();
  });
});
```

- [ ] **Step 2: Jalankan spec beranda**

Run: `cd dml-web && bun run test:e2e tests/e2e/beranda.spec.ts`
Expected: PASS, semua assertion hijau.

- [ ] **Step 3: Perluas spec JS-off**

Modify `dml-web/tests/e2e/no-js.spec.ts`, tambahkan test baru di akhir file:

```ts
test("beranda terbaca penuh tanpa JavaScript", async ({ page }) => {
  await page.goto("/");

  await expect(page.getByRole("heading", { level: 1 })).toBeVisible();
  await expect(page.getByText("Transportasi BBM")).toBeVisible();
  await expect(page.getByText("Penumpang Ro-Ro")).toBeVisible();
  await expect(page.getByText("Galangan Kapal")).toBeVisible();
  await expect(page.getByRole("heading", { name: "Perbandingan Armada" })).toBeVisible();
  await expect(page.getByRole("table")).toBeVisible();
  await expect(page.getByRole("heading", { name: "Rute Penyeberangan Ro-Ro" })).toBeVisible();
  await expect(page.getByRole("heading", { name: "Silsilah" })).toBeVisible();
  await expect(page.getByRole("link", { name: "Lihat silsilah lengkap" })).toHaveAttribute(
    "href",
    "/tentang-kami#silsilah",
  );

  const ctaLinks = page.getByRole("link", { name: /hubungi kami/i });
  expect(await ctaLinks.count()).toBeGreaterThan(0);
  for (const link of await ctaLinks.all()) {
    await expect(link).toHaveAttribute("href", "/kontak");
  }
});
```

- [ ] **Step 4: Jalankan spec JS-off**

Run: `cd dml-web && bun run test:e2e tests/e2e/no-js.spec.ts`
Expected: PASS.

- [ ] **Step 5: Verifikasi axe-core dan kontras token sudah mencakup beranda**

`tests/e2e/contrast-tokens.spec.ts` (Plan 2) sudah menguji `/` di antara empat route-nya, tidak perlu diubah. Tambahkan assertion axe di `beranda.spec.ts`:

Modify `dml-web/tests/e2e/beranda.spec.ts`, tambahkan di dalam `test.describe("beranda dengan prefers-reduced-motion: reduce"` sebagai test baru:

```ts
  test("tidak ada axe-core violation di beranda", async ({ page }) => {
    await page.goto("/");
    await runAxeCheck(page);
  });
```

Tambahkan import di baris atas file: `import { runAxeCheck } from "./axe";`

- [ ] **Step 6: Jalankan ulang spec dengan axe**

Run: `cd dml-web && bun run test:e2e tests/e2e/beranda.spec.ts`
Expected: PASS, termasuk tes axe-core baru.

- [ ] **Step 7: Pasang Lighthouse CI**

Run: `cd dml-web && bun add -d @lhci/cli`

Create `dml-web/lighthouserc.json`:

```json
{
  "ci": {
    "collect": {
      "startServerCommand": "bun run build && bun run start",
      "url": ["http://localhost:3000/"],
      "numberOfRuns": 1,
      "settings": {
        "preset": "mobile"
      }
    },
    "assert": {
      "assertions": {
        "categories:seo": ["error", { "minScore": 0.95 }],
        "largest-contentful-paint": ["error", { "maxNumericValue": 2500 }],
        "cumulative-layout-shift": ["error", { "maxNumericValue": 0.1 }]
      }
    },
    "upload": {
      "target": "filesystem",
      "outputDir": "./.lighthouseci"
    }
  }
}
```

Modify `dml-web/package.json`, tambahkan script dan masukkan ke `check`:

```json
    "lighthouse": "lhci autorun",
```

Ubah baris `"check"` jadi:

```json
    "check": "bun run lint && bun run typecheck && bun run test && bun run build && bun run doctor && bun run lighthouse",
```

Modify `dml-web/.gitignore`, tambahkan:

```
.lighthouseci/
```

- [ ] **Step 8: Jalankan Lighthouse CI**

Run: `cd dml-web && bun run lighthouse`
Expected: PASS sesuai ambang di `lighthouserc.json`. Kalau LCP di atas 2,5 detik, cek ukuran total frame hero (`du -sh public/media/hero-malam`), turunkan kualitas AVIF di `scripts/prepare-assets.ts` Task 4 Step 7 dari 55 ke 45 dan jalankan ulang `bun run prepare-assets`, atau kurangi jumlah frame yang dimuat sekaligus (`loading="lazy"` sudah dipasang untuk frame selain frame pertama di `night-sequence.tsx`).

- [ ] **Step 9: Jalankan `bun run doctor` sebagai pemeriksaan pola React**

Run: `cd dml-web && bun run doctor`
Expected: tidak ada blocking warning. Kalau ada temuan terkait `useEffect` di seksi 3D/GSAP (fleet-canvas.tsx, night-sequence.tsx, business-lines.tsx, lineage-pan.tsx, route-map.tsx, fleet-comparator.tsx, certifications.tsx), perbaiki cleanup atau dependency array sebelum lanjut, jangan suppress tanpa alasan tertulis.

- [ ] **Step 10: Gerbang akhir penuh**

Run: `cd dml-web && bun run check`
Expected: seluruh rantai (lint, typecheck, test, build, doctor, lighthouse) lolos dari kondisi bersih.

Run juga: `cd dml-web && bun run test:e2e`
Expected: seluruh spec e2e lolos, termasuk yang sudah ada dari Plan 1/Plan 2 (regresi nol).

- [ ] **Step 11: Commit**

```bash
cd /home/waxarsatia/test/company-profile
git add dml-web/tests/e2e/beranda.spec.ts dml-web/tests/e2e/no-js.spec.ts dml-web/lighthouserc.json dml-web/package.json dml-web/bun.lock dml-web/.gitignore
git commit -m "test: spec e2e beranda, Lighthouse CI, gerbang bun run check final Plan 3"
```

---

## Definition of Done, Plan 3

Seluruh poin berikut harus benar sebelum plan berikutnya (halaman `/bisnis/*`) dimulai:

- [ ] `bun run check` lolos penuh, termasuk Lighthouse CI mobile
- [ ] `bun run test:e2e` lolos penuh, nol regresi terhadap spec Plan 1/Plan 2
- [ ] `/kontak` dan `/karier` membaca nomor WhatsApp dari satu sumber (`COMPANY.whatsapp`)
- [ ] `README.md` memandu fresh clone sampai `bun run dev` berhasil dari database kosong, `docker-compose.yml` healthcheck terverifikasi mencegah race migrate
- [ ] `/halaman-tidak-ada` (404) render dokumen bergaya situs dengan `lang="id"`
- [ ] Beranda punya 8 seksi aktif: hero, potongan siang, tiga lini bisnis, fleet comparator, peta rute, silsilah, sertifikasi, CTA
- [ ] Beranda terbaca penuh dengan JavaScript dimatikan, semua CTA dan link hadir di HTML server
- [ ] Beranda dengan `prefers-reduced-motion: reduce` menampilkan seluruh konten tanpa animasi, tanpa canvas 3D dimuat
- [ ] Di bawah 768px, fallback blueprint SVG dan tabel spesifikasi tampil, bukan canvas WebGL
- [ ] `axe-core` tanpa violation di beranda
- [ ] Kontras seluruh elemen beranda lolos WCAG AA lewat `contrast-tokens.spec.ts`
- [ ] Tidak ada em dash di seluruh berkas yang dibuat plan ini
- [ ] Setiap angka fleet class yang tidak berhasil diverifikasi dari sumber real ditandai `// unverified`, dicatat untuk konfirmasi klien
- [ ] Tiga TODO `plan-bisnis` (hero, business-lines, cta-section) tercatat dan mudah dicari (`grep -rn "TODO(plan-bisnis)" dml-web/src`)
- [ ] **Tidak dikerjakan otomatis, perlu verifikasi manusia:** login ke `/admin` dengan browser sungguhan, konfirmasi collection `inquiries` read-only (dicatat sejak Plan 2 Task 16, belum ada browser tool di sesi manapun sampai plan ini)
- [ ] **Tidak dikerjakan, accepted tradeoff:** rate limiter `x-forwarded-for` tetap spoofable, sesuai keputusan review Plan 2
